# changelog

## 0.0.53

- judgment posture gains its first motivation mechanics since the 0.0.44 demotion, built from a full-ledger diagnosis of why delegation volume sits where it does (roughly 69 packages a day under coercion against 19 after it, a writes-per-dispatch ratio that swung from 0.46 to 19.9, 63 percent of substantial sessions with zero dispatches, and 805 of the 1395 post-demotion unverified-write advisories firing in sessions that had dispatched nothing): the unverified-writes advisory appends a decomposition prompt when and only when the session's dispatch count is zero, and a new once-per-session lanes reminder (`fleet-posture.session-lanes-reminder`) states the fan out default on the first prompt of a judgment session, tracked by a per-session marker, silenced by fleet mode off, and absent in strict which keeps its every-prompt reminder. both are additive advisory text; judgment posture still never denies a main loop write
- the verification recognizer's output-summary evidence finally speaks more than node:test grammar, which is the diagnosed root cause of windows never closing (52 verification credits against 10880 counted writes since 07-30, because the standing tail and grep piping discipline defeats exit-status evidence and the only output pattern was `fail 0`): vitest, jest, pytest and cargo single-line summaries now credit a piped run, and a grok review of the first cut caught that the go `ok <pkg>` and bare `N passed` fragments would credit a truncated FAILING tail whose failure lines scrolled out of the capture, so those two fragments are removed, cargo requires the same line to carry `0 failed`, and negative fixtures pin the truncated-failure shapes; go and playwright keep exit-status evidence when the runner is the final segment, and a dedicated verification-command suite exists for the first time
- the `gpt-5.6-sol` foreground write warning stops quoting a percentage frozen in prose: the hardcoded "41% in the last 7 days" (which also survived in the codex-cli-runtime skill text after the 0.0.50 sweep) is replaced by a live scan of the last seven days of job records that reports the measured timeout rate with counts at four or more samples and otherwise names the 570 second flight budget, recommending terra for quick shapes and luna for volume shapes. the scan applies the sol-foreground-write predicate before any cap after review showed the mtime-sliced 500 file budget could be consumed entirely by recent non-sol records, skips files over 16 MiB before reading, bounds total examination at 5000 files, stops at the seven day mtime boundary, and never throws. the motivating evidence rode one workspace where eight of eight dispatches went sol at xhigh, four timed out resumable, and zero were salvaged because direct slash commands sit outside the wrapper's scripted wind down
- the orchestration rules template encodes the routing policy edits the diagnosis surfaced as candidates: the inline work gate says to run the covering verification at each coherent boundary instead of batching it to the end, the dispatch gate states that width two is already a wave, the spec grade row requires a stated single-flight reason before a foreground write brief names sol, and auto invocation carries the decomposition-time default that dispatching independent packages together is the unmarked case; the rules manifest is regenerated for the hashed prose

## 0.0.52

- grok-4.6 becomes the scored grok flagship after its 2026-08-12 release: the capability row moves to grok-4.6 at 4/3/5 with notes carrying the measured shape (AA Intelligence 61 tied with gpt-5.6-sol behind Fable and Opus; Vals SWE-bench Verified 95.6 against 86.6 for grok-4.5 at roughly 604 seconds per task against 182 for sol; terminal-bench v3.0 26 against 34.6; 500k context unchanged; xhigh effort new and 4.6 only; cached input repriced from 0.30 to 0.50 short and 0.60 to 1.00 long per million), the grok-4.5 row is removed under the single flagship shape with 4.5 still available upstream, the sol row's stale "ties grok-4.5 on SWE-Bench Pro" cross reference now cites the Vals pair, and the four role policy is retained deliberately with an evidence window on 4.6 agentic gains instead of any ownership change
- the routing rules template follows the live listing: both "(it currently lists only grok-4.5)" parentheticals become "(it currently lists grok-4.6 and grok-4.5, neither a fast tier)", the example capability table mirrors the re-scored real one, the five extra codex listing models (codex-auto-review, gpt-5.3-codex-spark, gpt-5.4, gpt-5.4-mini, gpt-5.6-sol-wm) are recorded as deliberately unscored with no routing seat, and the rules manifest is regenerated for the hashed prose changes
- the grok contract epoch moves to 1.0.3 after the CLI auto-updated from the pinned 1.0.0: a source spot check of the 2026-08-12 snapshot confirmed every companion-facing surface unchanged (headless resolvers still drop --no-subagents; no bare --memory flag exists and GROK_MEMORY stays the headless control; --no-auto-update and --no-wait-for-background remain as hidden clap args, which is why they left the visible help; stop reasons stay snake_case; the sandbox event enum is the same six types with additive optional ProfileApplied fields; the version hard gate still runs at all three entrypoints), live probes covered consult, web, and write on the installed binary, and verified-versions, the README pair, the fake-grok default version string, and the "verified through" wording family move together; one source caveat is recorded here rather than as a doc claim: GROK_SUBAGENTS_MAX_DEPTH clamps to a floor of 1 and is not a kill switch, while GROK_SUBAGENTS=0 and the hard Agent deny remain the effective controls
- the first live /fusion:smoke wave runs and passes all five gates (version parity at 0.0.51, codex luna dispatch and envelope, grok dispatch and sandbox handshake on the new grok-4.6 default, worker deliverable, settlement write), closing the open item carried since 0.0.51 shipped

## 0.0.51

- the peer CLI epoch moves to codex-cli 0.147.0 and grok 1.0.0 after a seven facet research fleet plus live verification: the codex tested interval becomes `[0.147.0, 0.148.0)` with the contract doc corrected to what setup actually does above the ceiling (`ready: false` and exit 1, task and review preflight unaffected), `verified-versions.json` and the README move to the new pair, and live probes against the installed binaries confirmed task execution, thread resume, `--output-schema` structured output, and JSONL rollout persistence on 0.147.0 plus strict sandbox handshake, builder tracing, write mode, and web fetch on grok 1.0.0. the grok 1.0.0 major cut itself is routine, four releases of TUI and reliability fixes with no headless breaking change, no flag deprecation, and no ACP freeze statement, and the interval verdicts that matter are all confirmations: headless still never wires the CLI memory flags so `GROK_MEMORY` remains the effective control, `--no-subagents` is still not forwarded so the documented sentence stays true byte for byte, and the envelope comment says frozen for external tool compatibility
- grok 1.0.0 emits snake_case stop reasons and the companion's two `=== "Cancelled"` literals never matched them, which silently killed permission denial classification, historically the single most frequent grok failure kind; a live probe caught it because the suite could not, since fake-grok also spoke CamelCase. stop reasons are now normalized (case and underscores) before classification, the fixtures emit the live wire values with one deliberate legacy CamelCase case proving tolerance, and the max turns fixture stops emitting a marker event that the json output format has not carried since the streaming formats took it, leaving the stderr pattern as the detection path it already was
- a speculative `x_search` disallow added as defense in depth killed every non web run in live probing: grok 1.0.0 warns when a `disallowedTools` entry matches nothing, non web runs never arm the hosted tool, and forced builder tracing correctly fails closed on unmatched warnings. the entry is reverted, the lesson is encoded rather than remembered, fake-grok now prints the real unmatched disallow warning for unknown ids so the class is reproducible, and the docs state the actual boundary: the fixed `--tools` allowlist alone governs the toolset, live verified by a no web consult that self reported exactly `read_file, list_dir, grep`
- the usage invariant learns about cache creation before it burns anyone: grok 1.0.0 envelopes carry `cache_creation_input_tokens`, the companion's alias table did not, and a nonzero value would have failed `input + cacheRead + output === total` and rejected the entire usage object; the field now rides through the envelope parser, the observer's duplicate alias table, the fixtures, and the totals check, with a nonzero coverage case. found by a codex review of the diff whose 570 second timeout was salvaged by one scripted wind down resume that returned six findings, all six verified real against the tree
- deterministic child environment grows to cover what grok 1.0.0 actually reads: `GROK_WEB_FETCH` pinned by web mode since web fetch is separately gated upstream and off by default, `GROK_AUTO_WAKE` pinned false because the upstream default is true and a delete would have left it on, `GROK_MANAGED_MCP_GATEWAY_TOOLS_ENABLED` pinned false beside the managed MCPs pin, and `GROK_TEST_VERSION`, `GROK_SANDBOX`, `GROK_LEADER_SOCKET`, `GROK_SUBAGENTS_MAX_DEPTH`, `GROK_WEB_FETCH_PROXY`, `GROK_WEB_FETCH_ALLOW_LOCAL` removed, while the org version policy variables stay untouched because scrubbing them would bypass organizational enforcement; the doctor skill audits the whole set
- the codex rollout inspector finally runs in production instead of only in its tests: terminal records missing canonical observations now consult the rollout sidecar through the monitor's real path, the thread only contract replaces a `turnId` requirement that nothing ever wrote, a boundaryless rollout with more than one turn returns `ambiguous_turns` instead of attributing every turn's usage to the current job, effort is recovered from the `-c model_reasoning_effort=` form codex actually emits rather than a `--effort` flag it never does, and the argv scrape reads the codex child pid rather than the node wrapper's
- fixture realism becomes a stated goal rather than an accident: fake-codex moves its default to 0.147.0 and rejects the top level subcommands it does not model (doctor, plugin, app-server and friends previously fell through to the exec simulation and returned a fake success), fake-grok gains a `--version` branch printing the live hash suffixed format it never had at all, `FAKE_GROK_REMOVED_CAPABILITIES` lets tests simulate an upstream flag removal for the first time since the acceptance probe path used to exit 0 for any `--help` argv, and the org version policy death fixtures speak 1.0.x with the exact upstream sentence shapes
- the merged tree full suite stays the acceptance gate and re-proved itself twice in one wave: the stop reason casing switch and the cache creation field each rippled into suites outside every package's named verification list (`failure-kinds`, `stats`), including one deep equal whose missing trailing comma dodged a bulk replace, and both surfaced only on the full run. grok models currently lists a single entry, `grok-4.5`, and the codex listing carries five models the routing table does not score (`codex-auto-review`, `gpt-5.3-codex-spark`, `gpt-5.4`, `gpt-5.4-mini`, `gpt-5.6-sol-wm`), recorded as fact with re-scoring left to `/fusion:config`

## 0.0.50

- codex failure attribution stops collapsing three distinct deaths into one bucket: a seven day read of 175 engine jobs found 34 foreground wall clock kills, 41 percent of every `gpt-5.6-sol` write run against 15 percent for terra and near zero for consult, and their event streams split them into 19 genuine overruns, 12 runs stuck in an `apply_patch verification failed` retry loop and 3 that lost their exec child to `UnknownProcessId`. only the first 19 are a sizing problem, so the standing advice to split the brief was the wrong remedy for 44 percent of the family. neither failing string ever reaches the structured event stream, since events carry only `command_execution` and `file_change` items and a `file_change` is emitted only on success, so detection rides the child stderr that was already streamed live, counted on the arriving chunk with a carried partial line so a pattern split across two reads counts exactly once: `patch_thrash` fires on the third consecutive patch verification failure and a completed `file_change` resets the count, `exec_lost` on the third lost process, both terminate early through the same path a collaboration violation uses, and both join the repeated class in the breaker so one is a retry and two open it. what is saved is the wall clock rather than the label, so a run that genuinely reaches the deadline still reports `timeout`
- a deliberate kill stops losing its reason on the way to a terminal record: a collaboration policy violation exits through `cleanupRequired`, which leaves the record non terminal at `phase: cleanup-required`, and the reconciliation that finalizes it hardcoded `failureKind: "died"` over whatever was already recorded, so the attribution was destroyed exactly when it mattered and the resumable patch on the terminal path never saw a resumable kind. reconciliation now preserves a failure kind that is in the resumable set and falls back to `died` only when none was recorded, leaving the identity replaced and checkpoint pending paths converging on `died` as before. this is the same defect as the timeout bucket one entry above, a real cause overwritten by a generic one, and it surfaced only because the assertion meant to prove the resume footer could never pass
- three mechanical companion defects that each cost real work in the same seven days are fixed at their seams: codex refused to start three times in two days because the working directory had no ancestor `.git`, so a read only consult now auto passes `--skip-git-repo-check`, where the check carries no safety meaning, while a write fails fast naming the flag, and the recorded `request` keeps the user's declared intent rather than the inferred value so the inference never propagates down a resumed thread; a single flight bounce destroyed the staged brief twice, because the raw transport was consumed and unlinked at dispatch entry while the already running guard threw later from inside the task path, and the payload now survives until a job record is actually reserved; and the `gpt-5.6-sol` foreground write warning, which rode 58 records of which 34 succeeded, stops being written after `executeRecord` returns and becomes a preflight on the exact risk combination, carrying the measured 41 percent instead of a p90 claim
- a policy killed run becomes salvageable the way a timed out one is: `timeout`, `policy` and `patch_thrash` with a live thread all receive the resume footer and the wrapper contract's single scripted wind down matches the same set, after two runs lost 413 and 443 seconds of completed work with zero output because resumability was keyed to `timeout` alone. resumability and the breaker stay orthogonal, so `policy` still opens the circuit
- `/fusion:stats` stops reporting a structurally false unverified rate: every one of the 34 wall clock deaths was salvaged by a wind down resume that settled cleanly, 27 accepted and 6 rejected, but not one of the dead attempts is any worker record's `peerJobId`, since the wrapper binds only to the resume, so they could never receive a verdict by any existing path and sat at `unverified` forever, making one package read as two engine records. a terminal error record carrying a resumable failure kind, a thread id, no recorded verdict and a later same workspace job resuming that thread is now derived as `superseded` and counted in its own bucket; a recorded verdict always wins, and `superseded` is never accepted by `--record` nor written to disk
- the verification list lesson recurs one layer up, at the brief: a peer package reported `76 pass, 0 fail` while 64 process dependent tests, including every test it had just written, were skipped in its sandbox, because `node --test` exits 0 on a skip and the brief's done criterion accepted the exit code. the package was honest about the skip and two dispatches were still spent before the mandated full environment rerun showed `137 pass, 3 fail`. an implementation brief's acceptance criterion has to read `# fail 0` and `# skipped 0`, or name the titles that must appear passing, so that the work is done and the work is verified cannot both be claimed by one vacuous green

## 0.0.49

- the judgment posture unverified ceiling is removed entirely, by user directive, after its first week fired almost solely on false positives: the ceiling denied writes in sessions that were verifying constantly, because the 0.0.48 sensor's output evidence regex (`fail 0`) is node:test reporter grammar while the sessions that hit the stop live in vitest repositories (`Tests 1635 passed`), and the exit status channel requires the runner as the final unpiped segment, a shape the standing discipline of piping every run through `tail` or `grep` guarantees never occurs. three days of audit ground truth: 1126 counted writes, 18 verification resets, 25 ceiling denies across six sessions, every denied session demonstrably green on its own suite. the deny branch, `FUSION_INLINE_UNVERIFIED_CEILING`, the approaching ceiling advisory note, the `unverified-ceiling` audit reason and the stats `unverifiedCeilingStops` counter all leave; judgment posture counts and advises and never denies a main loop write, `strict` keeps every floor verbatim, and the two posture independent denials (no-op heartbeat Bash, reaped worker probe) stay. the vitest blind sensor itself is deliberately not repaired here, removal rather than recalibration was the directive, and it remains the open candidate since advisory counts still overstate in vitest repositories
- every user visible message now ends with a stable numeric digest, `[fusion:NNNN]`: a 43 entry registry in `lib/user-messages.mjs` maps each message kind to a 4 digit code derived from the sha256 of its slug, so the code survives rewording and can be quoted, searched and counted. guard denies and advisories, all worker lifecycle stop gate and dispatch messages, the fleet posture reminders, breaker advisories, rules sync notices, the compact echo, codex monitor lines and the stats raw args error all carry one. guard audit events stamp their `messageCode`, `/fusion:stats --messages` lists the registry and `/fusion:stats --message <n>` explains one code and shows its recorded occurrences, both admitted as strict direct arguments; message kinds without an audit backed ledger say so instead of pretending
- the tagging sweep re-proved the verification list lesson at larger scale: three suites outside every package's named verification list pinned exact message text (`codex-wrapper-contract`, `codex-jobs-monitor` with twenty pinned monitor lines, and the `raw-transport-surface` ritual regex over the stats skill), and all three surfaced only on the merged tree full run, which stays the acceptance gate for exactly this reason

## 0.0.48

- the inline guard's verification sensor stops being blind to real traffic: the recognizer only ever looked at a compound command's final segment, so `npm test 2>&1 | rg` shapes never registered and a seven day audit showed exactly 1 recognized verification event against 1325 counted writes, which is how the forty write ceiling fired on a session that had run the full suite green eight times that day. recognition is now any-segment with a quote-aware splitter, and the pass decision splits by observability: a runner that is the final segment keeps the exit status check, while a runner followed by a pipe tail or sequenced segment requires positive evidence in the tool output, a `fail 0` summary with no `✖` or `not ok` line, because a grep tail exits 0 even when it merely matched failure lines. each reset stamps `evidence: exit-status|output-summary` on its audit event and `/fusion:stats --audit` aggregates recognizer health by evidence kind. ten transcript vectors from the incident session ride as tests, five that must reset and five that must not
- writes outside the session workspace stop burning the unverified window: an absolute path outside the hook's cwd (memory files, raw-args transport payloads, scratchpad paths) is logged with `exempt: out-of-workspace` and does not count, while relative and missing paths keep counting, failing toward the count. the ceiling default stays at forty untouched, because the distribution it was calibrated on was measured under the blind sensor and deserves re-observation before any move. a latent `NaN` in the stats audit day aggregation for unseeded event kinds went with the schema work
- a five facet peer fleet staleness audit checked roughly four hundred living prose claims against the tree and twelve confirmed groups are corrected: the removed `FUSION_CODEX_INCLUDE_LEGACY` switch and `--include-legacy` flag leave the codex contract; the monitor sentences stop claiming pure observation, since the codex monitor lock-repairs a dead owner's running record through companion repair while stats stay read only and the grok observer stays advisory; the shared footer block documents that a `sandbox:` line appears exactly when a `job:` line does; `turn_limit` joins the documented grok failure kinds; the grok model capture order (envelope fields, sole `modelUsage` key, session summary) and the exact cost gate's `modelUsageIsIncomplete` condition match the implementation; and the README stops claiming no companion enforces version floors, since codex hard-fails preflight below its tested minimum. the audit's other thirteen candidates were filtered at synthesis as policy-versus-mechanism misreads, the third documented instance of that lane tendency
- six orchestration rule sentences align with what the code enforces: guards are bounded by the unverified ceiling and the two posture independent denials rather than "only" collection and verification; sizing precedence names the wall clock and stall overrides as the only surviving env knobs; Agent `run_in_background` names its two real exceptions, the review runner and a Fusion worker whose latest user message contains `--background`; the stop gate is described as advisory while work is in flight and blocking once results are terminal; `FUSION_ACCEPTANCE_EPOCH` is scoped to the anomaly report it actually gates; and the strict zero dispatch relief is stated as tool and path agnostic, with the workspace exemption qualified to absolute paths. one new sentence requires a warm thread follow up to restate `--model`, `--effort`, and `--cwd` like a fresh dispatch, after a live continuation composed a new engine call with no options and fell silently to the config default sol, whose foreground warning fired on the receipt and was overlooked
- grok terminal footers become symmetric with codex: `sandbox: <cwd>` rides exactly where `job:` rides, on done, error, cancelled, result re-render, and terminal status paths of recorded jobs, while pre-record input rejections keep carrying neither. the expectation sweep that followed reached `task-lifecycle`, `review`, and the `rules-sync` doc parity regex, two suites the package's own verification list had missed, re-proving that the orchestrator's full suite rerun before acceptance is the net that catches verification list authoring gaps

## 0.0.47

- an implicit working directory below a repository root now fails the dispatch instead of mis-rooting the sandbox: the 0.0.41 answer to the leaked shell cwd class was a warning attached after the run, and its next live occurrence showed why that could not hold, since the warning reached stderr only on the foreground path, never at background dispatch, and the result footer a collector reads never carried the workspace, so a codex run rooted at `apps/api` simply exited with zero changes. codex `task`, `review`, and `adversarial-review` and grok `task` and `review` now reject before any job record exists with the shared `input` failure kind and a message naming both remedies, `--cwd <toplevel>` to target the repository root or `--cwd <subdirectory>` to confirm the narrow root deliberately; an explicit `--cwd` of any existing directory always passes, a cwd outside any repository stays accepted, and worker re-entry, the stop gate, and the lookup subcommands are untouched. the codex warning surface and its `explicitCwd` stamping leave with it as dead code, and grok, which had no detection at all, gains the same gate with the toplevel resolved once and reused in the record
- every terminal footer now names its sandbox root: `sandbox: <cwd>` sits between `job:` and the delivery lines in both companions and the shared companion contract documents it, so a collector can always check where a run was rooted; the orchestration rules now require an explicit `--cwd "<absolute workspace root>"` on every dispatch that starts an engine run, in the current workspace as well as in a worktree, so a drifted shell fails loudly at dispatch rather than silently sandboxing to a subtree
- the test suite stops failing on environment denials that are not regressions: a sandbox that denies process inspection broke lock owner and identity verification for every job-creating test (17 such failures across the two package verifications in this very wave), and a host git config with an unreachable signer could abort fixture commits entirely. `processInspectionAvailable()` in the companion harness probes the same `process-identity` mechanism the companions use, `COMPANION_TEST_NO_PROCESS_INSPECTION=1` forces the denied path anywhere, and 128 job-creating tests across four suites now skip with `process inspection unavailable in this environment` instead of failing; `gitIsolation` becomes a function that injects `commit.gpgsign=false` at the environment level with a sandbox-local home and an empty template directory, and every direct git spawn in tests rides it, with the `rules-sync` runner left self-configured by design because it tests signed commits against its own throwaway key and local config outranks anything a host could set. measured: a capable environment runs 171 of 171 with zero new skips, the forced denial runs 43 with 128 reasoned skips and zero failures, and the full suite under a hostile `GIT_CONFIG_GLOBAL` matches the clean run exactly
- the 0.0.38 Opus 5 parity treatment is withdrawn: production use between 07-25 and 08-01 showed Opus 5 orchestrating well below Fable, so the rules preamble, the README `best[1m]` sentence, and doctor step 7 stop calling the seat lateral and name Fable the intended orchestrator with Opus 5 as a fallback to reverse through /model once Fable returns. what survives is deliberate: no posture, gate, fleet decision, or verification requirement conditions on the session model, and a session on the fallback keeps full width and narrows nothing, because 0.0.15 already demonstrated that softened rules collapse delegation

## 0.0.46

- the judgment posture write window gets a ceiling, because 0.0.45 removed the deny and left a counter with no terminal condition at all: the first real session under it ran a Base UI migration to 150 unverified main loop writes across 31 escalating advisories with zero dispatches, and guard audit dispatches went 116 on 07-29, 50 on the release day (the cache landed at 21:14), then zero the day after against 113 writes. `FUSION_INLINE_UNVERIFIED_CEILING`, eight budgets or forty writes by default and zero to switch it off, denies past that point; the last two advisories before the ceiling name the number so the stop is never a surprise, and the denial names both moves that reopen the window, a passing verification or a dispatch, so the demand predicate matches the acceptance predicate. forty is not a guess: across 31 measured sessions the window depth distribution is p50 7, p75 10, p90 30, then 60, 80 and 154, with the 40 to 49 band empty, so the ceiling sits in the natural gap and never fires on 27 of them. `strict` is untouched, where the five write budget deny still arrives first
- the worker ledger stops growing forever. terminal records and their sidecars expire after `FUSION_WORKER_RETENTION_DAYS`, ninety days by default and zero to keep everything, swept when the next task is created and never touching a record whose transport is still live. the point is not disk, since the ledger was only 12MB; it is that a retention horizon lets a compatibility shim for a retired record shape expire on a schedule instead of having to be argued away, which is why 1755 files had accumulated in fifteen days while the guard audit (180 days) and the codex monitor (30 days) both already had one. `/fusion:stats` gains an unverified ceiling stop counter in the coercion ledger
- `fusion:fast-worker` finishes retiring. the hook matchers, `EXECUTION_AGENTS` and `BRIEF_AGENTS` drop a subagent type that has had no agent definition since 0.0.43, while the `AGENT_TYPES` alias stays because 207 live ledger records still carry it and `/fusion:stats` has to keep reading them; one test now covers that alias deliberately, since the suite no longer exercises it by accident. the circuit breaker was the load bearing case rather than dead prose: its record filter matched only the retired lane, so `fusion:claude-worker` and `fusion:trivial-worker` silently lost breaker coverage the day fast-worker went away, and it now advises per live lane
- 63 exports that nothing imported anywhere are unexported, 19 of them from the inline guard alone, which is a hook script rather than a module API. `readManifestHashes` stays exported because `setup/SKILL.md` instructs the model to import it at runtime, a reference no code only scan can see
- five environment knobs that were declared, defaulted and never used are removed: `FUSION_WORKER_BRIEF_MAX_BYTES`, `FUSION_WORKER_MAX_TURNS`, `FUSION_WORKER_MAX_OUTPUT_TOKENS`, `FUSION_WORKER_MAX_UNCACHED_TOKENS` (the brief envelope's `sizing` field is the real control and keeps its scaling tests), and `FUSION_WORKER_DEBUG_COLLECTION_RESPONSE`, whose dump duplicated the `.final.txt` artifact. `FUSION_WORKER_WALL_CLOCK_MS`, `FUSION_WORKER_STALL_MS`, `FUSION_SETTLE_DEMAND_STALE_MS` and `FUSION_BREAKER_LOOKBACK_HOURS` were on the same list and stay, because each is the only seam that makes a long threshold reachable in a test. `FUSION_ACCEPTANCE_EPOCH` and `FUSION_FLEET_MODE` stay and are now documented, under the rule that a knob is either documented or dropped
- the peer contract is re-verified rather than renumbered: grok's required headless flag surface still holds at 0.2.117, where `--no-auto-update` and `--no-wait-for-background` are absent from `--help` but still accepted, so `verified-versions.json` and the README move to 0.2.117 while every statement about 0.2.112 behavior stays as written, since those describe when a behavior arrived and remain true. the README also records Claude Code 2.1.220, and `grok-composer-2.5-fast` leaves the model table because the live listing no longer offers it

## 0.0.45

- the inline write guard stops blocking in `judgment` posture: 0.0.44 demoted locus to judgment in the rules but left the mechanism denying writes with two escape routes, and live traffic shows why that could not hold, since a window with 4 writes and 1136 bytes left denied a new React component at write 6 of 10, denied the retry, and was only unstuck by setting an environment variable, after which the same session wrote 20 more files without incident; the guard now counts and advises there and denies nothing, so no escape hatch is needed
- `FUSION_INLINE_GUARD_MODE` is retired rather than documented: a gate that ships with an environment variable to switch it off is a gate that should not be blocking, and gate strength now has exactly one control, the posture dial. The audit `mode` field is derived from posture (`strict` records `enforce`, `judgment` records `advisory`) so the ledger schema and `/fusion:stats` keep reading unchanged
- the judgment advisory is framed by what is unverified rather than by dispatch counts, naming the lanes the remaining work could go to and never naming `/fusion:ultra`, because a session doing inline authorship has no evidence of three independent packages; the dispatch framed line stays in `strict`, where a dispatch is what actually reopens the window
- `strict` posture keeps every 0.0.44 floor exactly: five writes per dispatch opened window, three tail edits at most 1024 bytes to files already in the window, the zero dispatch relief of ten writes and 16384 bytes, new path Writes and NotebookEdit denied past that, and fail closed on unreadable guard state, while `judgment` fails open because an unreadable counter should never stop the work
- two denials are now posture independent, since neither rests on semantics a counter cannot see: a no-op Bash command while Fusion tasks are in flight, and a `TaskOutput` or `TaskStop` probe of a worker that already completed and was reaped. Both were configurable through the retired mode variable and are not judgment calls but provably useless calls
- `/fusion:stats` keeps measuring what the deny used to prevent: the coercion ledger gains inline sprawl windows (a window that passed twice the write budget without a verification or a dispatch) and the deepest unverified window a session reached, both derived from advisory depth so they stay populated now that judgment posture emits no denials
- deferred by judgment: the escalation axis stays write count rather than user turns since the guard has no reliable turn signal and reading the transcript on every write would buy a worse defect than it fixes

## 0.0.44

- the coercion layer is demoted to advice: the fleet stops being a default that narrow execution must argue its way out of (measured at 651 `fleet-decline:` justification lines against 40 actual `/fusion:ultra` convenings across 80 sessions, with 71 percent of observed dispatch waves at width one), `fleet-posture.mjs` stops injecting a reminder into every prompt and fires only once the inline guard has observed repeated width one waves, and the ultra skill reframes width as a judgment call whose only real defect is dispatching independent packages one per turn instead of together
- a passing verification now reopens the main loop write window, so demonstrating that inline work is sound buys the same latitude as handing it off; the recognizer lives in `plugins/fusion/scripts/lib/verification-command.mjs` (test, lint, typecheck and check runners across the common toolchains, extensible per project through `FUSION_VERIFICATION_PATTERN`), the guard is registered on Bash `PostToolUse` to observe it, and the reset touches write accounting only so wave width and narrow wave telemetry stay honest
- the inline relief stops punishing sessions that delegate: the ten write and 16384 byte allowance no longer requires a session with zero dispatches and is measured within the window rather than across the session, which previously left a session with seventeen dispatches less inline latitude than one with none
- the write deny states what is unverified instead of commanding a dispatch, and its lane hint names `fusion:claude-worker` rather than the `fusion:fast-worker` seat retired in 0.0.43
- a posture dial makes the forcing explicit and discoverable through `/fusion:config set-posture <judgment|strict>` with `FUSION_POSTURE` as the per session override: `judgment`, the default, lets the main session choose width and locus per package while the guards enforce only collection and verification, and `strict` restores the previous mandatory floors for long unattended runs
- the orchestration rules replace the micro step gate (one file, twenty lines, spent on first edit) with an inline work gate sized by coherence: one change that a single same turn verification command covers, reopening every time that verification passes, with accumulation triggers narrowed to unverified accumulation, a failed verification, and a repeated symptom instead of raw edit and turn counts
- `/fusion:stats` gains a coercion ledger reporting what the gates claim to prevent: fleet declines against fleet shaped waves, verification resets, unverified accumulations, and the posture mix, so a miscalibrated gate surfaces as a number instead of living only in prose
- the Grok token usage observer stops being its own monitor: it never emitted a line, so registering it as one occupied a notification slot that only ever spoke to say it had stopped, and it now runs as a library inside Fusion's own monitor process (single slot `engine-jobs`, described as Codex job completions and peer engine token observations), with the Grok observation isolated at the call site so it cannot skip a Codex announcement or kill the interval; polling, the per job workspace keying, the state file, and the `GROK_JOBS_OBSERVER_UNAVAILABLE_TTL_MS` bound are unchanged, while the dead `GROK_JOBS_OBSERVER_INTERVAL_MS` knob is retired in favour of the monitor's own interval
- the `fusion:claude-worker` agent frontmatter parses again: its unquoted description contained `claude-fallback: <reason>`, which a YAML plain scalar cannot hold, so since 0.0.43 the whole frontmatter was silently dropped at load and the agent ran without its model pin, turn budget, or `disallowedTools: Agent` restriction

## 0.0.43

- codex adapts to cli 0.146.0: the tested interval moves to 0.146.0 up to but excluding 0.147.0 with the preflight minimum at 0.146.0, version parsing tolerates alpha hotfix suffixes like 0.146.0-alpha.10.1, and the contract records the 2026-07-29 live verification (task execution, resume, structured output, rollout persistence, native review) plus a probe confirming native review still accepts and ignores `--output-schema`
- grok adapts to cli 0.2.112: the capability preflight generalizes its acceptance probe to every required flag hidden from `--help` (previously special cased for `--no-auto-update` and `--no-wait-for-background`), a hard version policy death (organization message on stderr, exit 1 before any envelope, from 0.2.112's soft and hard version policy split that `--no-auto-update` cannot bypass) now classifies as `setup` with remediation guidance matched to the violated bound (upgrade below the minimum, install an approved build above the maximum), and setup reports an informational `[shell_environment_policy]` presence advisory
- the grok contract records the 0.2.110 to 0.2.112 audit: stationarity auto stop surfaces as a silent EndTurn completion, `-r` additionally accepts session titles while the companion still passes only UUIDs, 0.2.112 memtrace diagnostics resolve the real home independent of `$HOME`, and a controlled probe shows `--tools x_search` unmappable (fail closed under forced tracing) while the backend tool works from the full toolset, keeping the agent profile wiring stance
- verified engine pins move to codex-cli 0.146.0 and grok 0.2.112; the rules drop the stale `--background-wait-timeout` fallback wording (removed from the companion in 0.0.35), the doctor skill globs the skills directories instead of the removed commands layout and sheds its stale best-of-n surface description (retired in 0.0.35), and every surface still describing pre split version enforcement (SECURITY, README, both rules docs, the grok runtime skill) aligns to the 0.2.112 soft and hard model
- the claude execution worker is re-formed: `fusion:fast-worker` becomes `fusion:claude-worker`, a claude-surface-only seat (hooks, subagent files, MCP, plugin state, privacy boundary, or a structurally stranded package carrying a `claude-fallback: <reason>` header) with no general fallback role, its briefs adopt an artifact-first deliverable contract, width one dispatches may run synchronously through `run_in_background: false`, and the lifecycle accepts both names so existing records keep settling (mining showed the old lane was the slowest at p50 514s versus terra 236s, luna 77s, grok 107s, with only 6 percent of lifetime briefs needing the claude surface)
- settlement grammar sheds its ordering trap: `--reason-for` binds by record id from any position, a bare `--reason` broadcasts to every rejected pair without a per pair reason, and every grammar error appends a correct usage example (40 live contract errors in three days motivated the change)
- the stop gate becomes wave aware: collected but unsettled records stop demanding settlement while same session workers are still in flight, batching one multi pair command when the wave drains, with `FUSION_SETTLE_DEMAND_STALE_MS` (default 1800000) forcing the demand for stale leftovers
- a wrapper that dies on a harness API error before the companion created a job auto settles as rejected with a redispatch advisory naming the persisted brief, and the narrow wave advisory counts same message pending launches so a batched wide wave can no longer read as consecutive narrow waves
- grok ordinary task mode accepts the same `--json-schema` contract as review, consuming `structuredOutput` with identical failure semantics

## 0.0.42

- the codex rescue wrapper salvages foreground timeouts itself: a companion result ending `state: error` with `failure: timeout` and a `Resume Codex job` line authorizes exactly one scripted resume carrying a fixed wind down prompt, run as one additional foreground Bash call and relayed verbatim in place of the first output, never chained; the exception lands consistently in the rescue agent contract, the result handling, cli runtime, rescue, and task skills, the codex contract doc, the orchestration rules, and troubleshooting (15 of 20 recorded timeout deaths had ended salvage accepted only after a manual resume round; the wrapper now spends that round itself)
- quick tier briefs get a time budget: the quick scoped routing row sizes a brief to one 570 second foreground flight, with the narrow suites for the touched files in the verification command and the complete list reserved for the orchestrator's collection rerun, an oversized package consolidates or splits before dispatch, and a timeout that survives the single scripted resume is a sizing error, not a redispatch candidate; the trivial row names the light middle band (explicit recipe single subsystem fixes, doc sweeps, mechanical test updates) as gpt-5.6-luna work, reserving terra for quick packages whose edits span files or need judgment inside the recipe
- job records carry their runtime provenance: codex records persist `timeoutMs` and `companionVersion`, grok records and fusion worker ledger records gain `companionVersion`, each plugin reading its own manifest through a cached `CLAUDE_PLUGIN_ROOT` aware helper; persisting `timeoutMs` makes the 0.0.41 near cap counter count near cap finishes for real (it had been reading a field no writer ever set), and the version stamps make release watch mining sliceable by plugin version instead of timestamp inference
- fusion stats reads real engine acceptance: `semanticAcceptance` prefers `semanticStatus`, the key engine records actually write, over the worker ledger's `acceptance` key, so byAcceptance, lane signal drift, and the accepted with error transport anomaly stop being blind to engine records; the per SKU trend adds a `salvaged` column counting timeout deaths later accepted, and the anomaly excludes designed timeout salvage while still flagging every other failure kind
- the breaker advisory carries proportion: `latestBreakerFailure` also returns the failing kind's count and the engine's terminal job count inside the lookback window, and the advisory renders `(<n> <kind> across <m> terminal jobs, <h>h window)` so a singleton failure reads as a singleton instead of a lane wide alarm
- stop gate demand and satisfaction share predicates, round two of the 0.0.39 class: `runtimeTaskMissing` and `needsCancellation` are extracted as exported predicates evaluated by both the demand filters and the reverify pass, reaped settlement funnels through one `settleReapedIfActive` shape from both the stop partition and the no task found consumption site, and the duplicate `unverifiedCollectedWorkers` folds into `settleOnlyRecords`
- `/fusion:smoke` runs a post update live smoke wave before real work rides a refreshed install: version parity between installed caches and the marketplace manifest, one tiny probe per dispatch lane, and settlement recorded through `/fusion:stats` as one of the tested gates; the readme commands table and layout bullet list it, and the contributing release checklist runs it after every cache refresh

## 0.0.41

- the worker token budget stops eating deliverables: a one shot wind down lands at 85 percent of the output token budget telling the worker to stop calling tools and write the deliverable, and after the limit trips exactly one final `Write` is still permitted (`terminalWriteGraceUsedAt`) while every other tool stays denied with the deny reason naming the grace; the subagent start announcement and all three worker prompts state both (observed live: three of four mining workers lost their final report write to the old hard deny)
- settlement grammar sheds its two most repeated stumbles: a bare `/fusion:stats` invocation now renders the plain report because the empty argument list joins the value free direct family, and a single `--record` flag accepts multiple `<id>=<verdict>` pairs identically in the strict direct family and the raw args transport parser (seven contract errors across four sessions in one mining window motivated both)
- codex telemetry gets discriminating about time: per SKU trends add a near cap share (timeout deaths plus finishes within 30 seconds of the 570 second deadline), terminal ledger records carry `timeoutMs` and `failureKind` so ledger recovered timeouts stop understating `timeoutShare`, and peer wrapper ledger records derive `packageType` from an explicit `package-type:` line, then a `--write` or `--transport-default-write` token before the `--` separator, then the verification heuristic, instead of blanket `consult` (120 of 131 live jobs were mislabeled)
- grok reaches observability parity: the jobs observer emits `model-audit.jsonl` rows for terminal grok jobs (`appendModelAuditObservation` moves to `fusion-stats.mjs` exports) and its token rows gain `repositoryKey`; `--prune-dead` also sweeps orphaned `acceptance.jsonl` sidecars, dead since the 0.0.27 settlement cutover bypassed their writer and 0.0.35 deleted it
- the codex companion survives its two observed run killers: a job whose cwd resolves inside a repository subdirectory without an explicit `--cwd` gets a warning diagnostic plus stderr line instead of silently sandboxing to the subtree (two zero delivery rejections came from a leaked shell cwd), and an oversized JSONL event is skipped with one diagnostic while the run continues, failing as `resource` only when the protocol cannot complete after a skip, which also removes the broken pipe exit 143 race; both contract docs restate the behavior and the stale `--record-acceptance` reference is gone
- the narrow wave advisory becomes decline aware: when the transcript tail shows a `fleet-decline:` line after the last user message the stdout nudge is suppressed while the audit warn records `declineStated: true` (19 of 27 live advisories were firing at sessions with stated declines); the stop gate rereads cancellation records from disk before demanding `TaskStop` so a worker that turned terminal after the snapshot is no longer demanded, and the task id splice tolerates leading blank lines in an envelope brief
- breaker-check tests are hermetic: the suite builds a sandbox home with stripped `FUSION_`, `GROK_`, and `CODEX_` inheritance so live lane failures can no longer redden a local run, and the orchestration rules state that claude worker briefs are envelope first (`fusion-brief: v1` on line one, header prose after) with the rules manifest regenerated

## 0.0.40

- routing becomes capability first: a package routes by its load bearing capability instead of its surface task type, capability monopolies hard route ahead of every task type default (X and live platform reads to grok `live-web` even when digest shaped, Claude tool surface and privacy to Claude workers, judgment dense authorship to the main session), and lane defaults are named as cached capability observations that fresh observations override per package; grounded in a same day controlled probe where grok `web_fetch` returned a full x.com post body while the codex web surface returned empty HTML for the same URL
- the orchestration policy gains a judgment dense authorship section: a deliverable whose faithful brief would have to contain the deliverable itself (public naming, API shape, UX copy, rules text, a design's first exemplar) is authored in the main session as control plane work, the judgment density to volume ratio decides the split, design intent travels to peers as contract (exemplar files, MUST NOT gates, signatures, snapshots) rather than prose aspiration, and lane acceptance rates are never evidence of taste; the interactive design carve out generalizes to this rule, codex briefs gain a lean and gate shaped requirement (the vendor system card documents intent overreach through permissive instruction reading, worse under persistence emphasis), the grok design gate now points at the authorship rule instead of a taste score threshold no code reads, and readme, contributing, and troubleshooting restate the new invariants
- judgment shaped rejections become measurable: `/fusion:stats --record` accepts `--failure-kind` and per pair `--failure-kind-for` (raw args transport, rejected pairs only, one of `intent_override`, `scope_rewrite`, `wrong_approach`, `style_mismatch`), the engine acceptance bridge forwards the kind to both companions, `record-acceptance` on both engines validates it and now requires `--reason` for every rejected verdict (previously optional at the engine layer, which left 41 of 63 ledger rejections reasonless), worker records persist `acceptanceFailureKind` through queued verdicts, and the brief envelope accepts `package-type: design`
- the bench suite gains T09-style-fidelity, the first task measuring adherence to an externally imposed style contract as distinct from output quality under a model's own defaults: six machine checkable gates that deliberately conflict with model default habits (snake_case exports, promise chains with async and await forbidden, no classes, errors only through `app_error`, zero comments, named exports only), a style checker independent of the hidden behavior tests, and a mutant that passes every behavior test while violating only the contract, so the verifier isolates style fidelity; methodology and readme drop the stale single task claims and the capability table rows stop citing the nonexistent "bench B2" for taste
- `docs/grok-contract.md` documents X content access: x.com reads work today through consult `web_fetch` and are treated as a grok lane monopoly in routing, while the first party `x_search` backend tool rides the agent profile surface rather than the `--tools` allowlist and stays unwired until upstream documents headless enablement

## 0.0.39

- the over budget stop gate settles instead of looping when the harness has already reaped a worker's runtime task: a `cancel_requested` record whose runtime id is absent from a provided `background_tasks` array settles as `task_reaped` through the same `settleReapedWorker` helper the no task found escape hatch uses, because that hatch never fires for this class; `TaskStop` on a reaped id returns a tool_use_error, which emits neither PostToolUse nor PostToolUseFailure, so the demand was unsatisfiable (observed live: two user interrupted workers held eight consecutive blocking stop rounds while twelve errored `TaskStop` and `TaskOutput` calls delivered zero hook events, and only SessionEnd closed the records)
- reaped settlements keep the stop hook's single output document invariant: the notice rides the block reason while live cancellations or missing runtime failures still demand, and emits alone only when nothing blocks, so a blocking decision can never be lost to a second stdout document
- PreToolUse stamps `cancelAttemptCount` on matching non terminal main session workers when `TaskStop` or `TaskOutput` targets their runtime id, and a stop input without any `background_tasks` array settles a cancellation after two unobserved attempts, covering harness inputs that omit the runtime task list

## 0.0.38

- Fable and Opus 5 are peer orchestrators: the routing preamble drops the Opus as fallback framing (switching is lateral; posture, gates, fleet defaults, and delegation never condition on which of the two is active), the readme reframes `best[1m]` floating as lateral within the top tier, and doctor treats a deliberate Opus 5 session as healthy instead of a fallback to recover
- the stop hook's in flight advisory closes a cross process staleness window: candidate records are fresh read and filtered through the existing terminal collected predicates immediately before signing and emission (`reverifyInFlightRecords`), so an overlapping stop pass can no longer announce a task as still in flight after its notification driven terminal flip landed
- `/fusion:stats` direct arguments gain a second closed family: value free report and maintenance requests (`--json`, `--audit`, `--all`, `--prune-dead`, each at most once) pass without the raw-args transport; the settlement family, the rejection message, and the transport requirement for `--session` and all free text are unchanged, and the skill carve-out wording now matches the predicate verbatim

## 0.0.37

- settlement and telemetry become more discriminating: batched settlement validates every pair before writing, reports per pair rejection reasons, and `/fusion:stats` adds lane drift, per SKU trends, narrow wave watch, and an acceptance epoch cutoff; the worker lifecycle adds a parent context advisory, package type and brief byte capture, recalibrated trivial worker budgets, and token observations, while the inline guard gains tail allowance and softens zero dispatches
- repeated advisory noise is reduced across the lanes: in flight stop notices dedupe, the Grok token observer gains monitor silence, and the Codex monitor stays silent when no announcement is needed; verification also surfaces manifest lint advisories and codifies wave settlement and dispatch norms
- Codex now warns when Sol runs in the foreground, surfaces timeout resume state, and records `repositoryTopLevel`; Grok reports permission denial detail and the same repository identity, with peer wrapper completion contract canaries covering the delivery gate
- the bench suite completes all eight tasks, closing the release's orchestration, observability, transport, verification, and lifecycle coverage

## 0.0.36

- peer transport wrappers get their own completion contract: 0.0.35's SubagentStop matcher expansion routed `codex:codex-rescue`, `grok:grok-rescue`, and `grok:grok-review-runner` through the claude worker deliverable gate, whose `verification` contract demands a final message ending in `delivery: complete` plus `verification: passed`; a verbatim companion relay ends with the engine envelope (`job:`, `state:`) instead, so every foreground peer delivery burned its single truncation recovery retry arguing with the gate, took a false `failureKind: delivery`, and terminalized as `incomplete`, which the notification reconcile guard skips, breaking notification driven auto collection and forcing a stop collection block on every peer package (observed two of two in the first 0.0.35 session against twenty nine of twenty nine clean on 0.0.34 the same day); dispatches now record a `transport` contract, `completedReport` accepts a relay whose footer parses through `peerJobIdFromCollectedResult` (the same parser settlement identity backfill uses, with `grok:grok-review-runner` always deliverable because its terminal output may be raw json validated by the review consumer), a matching transport retry message covers genuinely truncated relays, and the guards also match the agent type so records created before the fix settle correctly after a hot deploy

## 0.0.35

- codex adapts to cli 0.145.0: task runs forward `--output-schema <path>` end to end (rescue raw args, exec argv, structured output capture, and a `structured: parsed/invalid/unavailable` footer in the render), adversarial review gains a json schema verdict contract at `plugins/codex/schemas/adversarial-review-verdict.schema.json`, native `review` rejects `--output-schema` because tested versions silently ignore it there, and the 0.144.4 models cache schema drift workaround is deleted with the tested interval moving to 0.145.0 to 0.146.0
- grok adapts to cli 0.2.110: setup probes the new `grok doctor` capability and surfaces it as a setup advisory line, and ordinary managed runs now require `--no-wait-for-background` unconditionally, dropping the `--background-wait-timeout` compatibility fallback and its capability probe
- best-of-n is retired: the tournament skill is deleted and grok's protected roles shrink to four (burst, independence, live-web, large-context) across rules, skills, agents, docs, and tests
- settlement is centralized: engine acceptance writers move to a shared `engine-acceptance.mjs`, `/fusion:stats` records every verdict through the one `--record <id>=<verdict>` form (`--record-acceptance` and `--record-worker-acceptance` are gone), and a verdict recorded before a worker's record turns terminal queues instead of failing, settling automatically when the record lands
- the worker lifecycle settles queued verdicts on terminal transitions with the engine cascade, backfills peer identity from rescue wrapper stops (the SubagentStop matcher now also covers `codex:codex-rescue`, `grok:grok-rescue`, and `grok:grok-review-runner`), and guards terminal writers against reprocessing already settled records
- legacy codex plugin state support is dropped: the `codex-openai-codex` compatibility root, `FUSION_CODEX_INCLUDE_LEGACY`, legacy `completed`/`failed` terminal statuses, and log tail failure kind recovery are removed from stats, breaker, and monitor
- the inline delegation guard always normalizes persisted dispatch counters instead of branching on a legacy period counter
- verified engine pins move to codex-cli 0.145.0 and grok 0.2.110

## 0.0.34

- the settle-only advisory stops repeating itself: when settlement is legitimately deferred (a package's verification waiting on a sibling landing), the pending-verdict block re-emitted identically at every stop boundary with no new information; it now dedupes by a persisted pending-set signature, the same mechanism the in-flight advisory has used since 0.0.31, emitting once per distinct set and again only when a record enters or leaves; blocking collection demands are unaffected and refresh the signature when they emit combined

## 0.0.33

- notification collection reads the transcript the notification names: every peer wrapper record in a live traffic window had a null `transcriptPath`, so the completed path extracted nothing, mislabeled clean consult wrappers (nine to eleven turns of a sixty budget, backing peer jobs all clean) as `turn_limit` incomplete, demanded manual raw transcript reads, and skipped peer footer seeding; the reconciler now stamps the record's `transcriptPath` from the notification's own output file element before transition, and a no-text completion classifies `missing_final_text` unless real turn evidence supports `turn_limit`
- the collection demand is always satisfiable: the old matcher required a full file read of a transcript that can exceed the Read tool's token cap, a physically impossible demand observed re-firing at every stop boundary for an hour; any Read of the record's `outputFile` (or `transcriptPath` when no artifact exists) now marks collection regardless of offset, limit, or a size-refused response, the demand wording drops the full-read requirement, and the final text artifact caps shrink from 192KB to 72KB so a maximum artifact always fits one read
- stray probes against reaped workers are redirected: the guard's PreToolUse now intercepts `TaskOutput` and `TaskStop` aimed at a terminal fusion worker and points at the record's output file and settlement command instead, ending the "No task found" error class the harness's one minute wrapper reap produced
- peer footer capture runs on incomplete completions too, so `peerJobId` seeds even when extraction fails and an engine job id settlement propagates to the wrapper ledger row
- workers get a wind-down instead of an overshoot: at two turns before the budget the lifecycle injects a final-deliverable instruction once per record, replacing the reactive cap that let workers run to sixty-two and sixty-four of sixty
- stop output consolidates: multiple pending records emit one combined block, and rows that are collected but unsettled carry a `settle-only:` prefix so the two obligations read differently

## 0.0.32

- worker agent turn caps stop undercutting their budgets: the harness enforces frontmatter `maxTurns` as a hard cap that ends an async worker silently as completed mid run, and `fast-worker`'s 40 sat below its own 60 turn base budget, so standard and large briefs were killed with no deliverable while the honest budget classifier watched a limit that could never fire; frontmatter now carries the doubled large sizing values (120, 60, 24) and a consistency test pins them to the budget table
- notification driven collection produces a readable deliverable: the task notification completed path extracts the final assistant text from a bounded transcript tail, writes the bounded final text artifact, and points `outputFile` at it, matching the `SubagentStop` path; a transcript ending on a dangling tool result with no final text classifies as `incomplete` with `turn_limit` instead of done and collected, so a truncated worker no longer reports as a success with nothing to read
- the wrapper footer is parsed on the notification path too: peer job identity seeds from the extracted final text for rescue wrappers, so an engine job id settlement propagates to the wrapper ledger row instead of leaving it demanding a second manual settle
- fleet wave width measures launches, not completions: the guard records dispatches at `PreToolUse` launch time and confirms them at `PostToolUse`, because async completion delivery arrives minutes after a batched launch and made every genuine wide wave read as staggered solo dispatches, inflating the narrow streak counter to nine in a session whose transcript showed batched waves throughout; wave derivation counts confirmed entries only, launches that never confirm expire after fifteen minutes, a missing launch recovers in place with a warn audit event instead of silently leaving the write budget exhausted, and the narrow wave advisory re fires at every even streak milestone
- `collectionMethod` is one enum: writers canonicalize to lowercase snake case, legacy spellings fold on load and in stats reads, because four different literals were live in one ninety minute production window
- codex acceptance carries provenance: `record-acceptance` stamps `acceptanceSource` and `acceptanceRecordedAt` on the job record, every writeback path enforces the `--accept-failed-transport` gate, and fusion-stats threads `--source` through the companion argv, closing a split brain where the fusion ledger said `collector` or `main-loop` while the codex record said `stats`
- stats gains an `--unsettled` view listing terminal jobs without an explicit verdict and renders acceptance provenance when present
- grok `turn_limit` survives into history output instead of coercing to null, the review runner treats an empty raw request as the default managed review instead of stopping to ask, and both rescue agents relay companion stdout verbatim in a fenced block after a production wrapper retyped `foreground` as `preground`
- the orchestration rules describe the async wire shape without weakening obligations: a launch receipt is not delivery, completion notifications arm mandatory collection, and a dispatch whose receipt is never followed by same goal collection and verification is the contract failure; new rules cover settling consumed reviews, a broad verification pass after any timeout salvage in a wave, and checking the codex single flight slot before dispatch; troubleshooting documents the two worker truncation signatures with the SendMessage recovery recipe, and the README development section names the bench manifest check CI already runs

## 0.0.31

- the fleet default gets a per turn signal: a new `fleet-posture.mjs` UserPromptSubmit hook injects one advisory context line each turn naming the three package threshold, `/fusion:ultra`, and the `fleet-decline: <reason>` protocol, because the default previously lived as one sentence deep inside a document loaded once at session start and fired almost never; `FUSION_FLEET_MODE=off` or a `fleet-mode` state file disables it, and the compact recovery echo now restates the fleet default at the moment of maximum context loss
- the orchestration policy stops competing with itself at its own trigger: the fleet default is hoisted into the operating model with an explicit split between the posture machine, which governs what the main loop touches, and execution width, which the fleet owns; the three manual fan out sites now name `/fusion:ultra` as the action for three or more independent packages, declines require a visible `fleet-decline: <reason>` line whose premises reopen the question when they expire, and consolidation no longer lowers the fleet decision's package count
- the ultra skill reads as the default posture it is: `when_to_use` leads with task shape instead of explicit asks, the facet count follows the goal's package count with a floor of three, skipping a fleet shaped goal is a stated decline, and the convening ceremony of checkpoint, worktrees, and sibling declaration lists runs inside the skill instead of being prepaid by the main loop
- the inline guard notices narrow dispatching: completed dispatch waves are bucketed by `FUSION_FLEET_WAVE_GAP_MS` and a second consecutive wave of width two or less appends one advisory line naming `/fusion:ultra` and the decline marker; advisory only, no permission decision changes
- the in flight stop advisory stops repeating: it is deduplicated by the sorted in flight task id signature persisted in a new per session state file, so a dispatch wave produces one armed notice instead of one per turn end, each of which cost an extra model request
- worker records reach terminal from delivered notifications alone: `handleStop` scans the parent transcript's task notification delta with a persisted offset, bounded per pass and fail open, before cancellation classification, flipping owned records to `task_notification` collection or a terminal failure; this removes the deliberately failing `TaskOutput` probe from the normal collection path and stops the gate demanding `TaskStop` for tasks that already finished

## 0.0.30

- claude worker results are collectable again: a worker's persisted `outputFile` was the transcript symlink, observed at 538KB against the `Read` tool's 256KB cap, so the gate demanded a read that could not be performed and every claude worker record stayed non terminal and therefore unsettleable; `SubagentStop` now writes a bounded final text artifact (head, truncation marker, tail, capped at 192KB) and points `outputFile` at it, while the launch response transcript path is kept only as `transcriptPath`
- the no activity budget stops punishing reconnaissance: read only tools refresh a new `lastLivenessAt` so a worker reading and searching is no longer classified as stuck, a tool call recorded as `inFlightSince` suspends the stall check so a worker blocked inside one long running call survives, and `lastProgressAt` with `progressEvents` keep their execution progress meaning for telemetry; the wall clock budget still bounds a genuinely hung call
- successful completions stop reporting as failures: a stale `failureKind` is cleared when a worker completes rather than retained for every kind except `unexpected_async`, and the superseded value moves to `recoveredFailureKind` so budget tuning keeps the signal; a freshly evaluated budget failure still wins at the terminal event
- the collection gate and the collection matcher agree: `terminalCollectionPending` demanded collection for any terminal async record while the `Read` branch accepted only `ready_uncollected` and the reap branch skipped terminal records, deadlocking the stop gate with a demand nothing could satisfy, so the matcher now reuses the gate predicate instead of restating it
- collected results are delivered once: a successful worker whose final text artifact was persisted is collected at `SubagentStop` as `subagent_stop` instead of waiting in `ready_uncollected` for a read of bytes the harness already delivered, and the semantic verdict gate still blocks the turn until `fusion-stats` records an outcome; workers without an artifact, collector contracts, and reaped records keep today's demands
- worker briefs declare their own size: an optional `sizing:` field accepting `small`, `standard`, or `large` scales the tier budgets by half, one, or two, with environment overrides still taking precedence
- delivery contracts ask for a verdict envelope: `SubagentStart` instructs execution and coverage workers to keep the final message to changed paths, verification counts, environment findings, and a path to any long report, and the delegation rules carry the same requirement for hand written briefs, because an inlined diff is carried in the orchestrator's context for the rest of the session
- the codex rescue transport ritual reads the allocated file once before writing it, matching the grok surface, because the harness refuses to overwrite an existing file that was not first read and the previous no read posture made every dispatch fail at staging; the transport surface test drops the codex specific carve out so both surfaces are held to one ritual
- the model drift audit filters by lane, so grok and claude tier SKUs are no longer reported as configured but absent from the codex listing
- process group signals are identity gated at the last two ungated sites: the fusion collector refuses to signal after its own child has exited, and the grok exited leader cleanup takes the captured identity and declines a recycled pid group
- the acceptance companion resolvers reject a relative or non regular `FUSION_GROK_COMPANION` and `FUSION_CODEX_COMPANION` override and gain cache layout hermeticity coverage, closing the last untested cross plugin resolution
- fixture heavy test authoring routes to the codex quick tier or `fusion:fast-worker` by default, with grok admissible only when fixtures arrive pre built, recorded against the turn limit and timeout family it produced
- the test matrix sets `fail-fast: false` so one flake no longer cancels the other three platforms

## 0.0.29

- the codex jobs monitor survives the installed plugin layout: the static cross plugin import of the codex companion (valid only in the repo checkout) becomes a startup time resolution that tries the `FUSION_CODEX_COMPANION` override, the repo relative sibling, then the installed cache's versioned codex directories, and a failed resolution degrades the monitor to announce only mode instead of crashing every new session; a cache layout hermeticity test reproduces the installed shape so this defect family cannot land silently again
- the Stop gate advisory honors `stop_hook_active`: the in flight armed collection advisory and the acceptance advisory emit once per stop chain and then return silent success on hook driven continuations, ending the advisory ping pong that re woke the model until the harness's nine block override cap fired; blocking outcomes (cancel demands, terminal uncollected collection, collector gates) are unchanged

## 0.0.28

- advisory stop rounds stop consuming the cancellation budget: a healthy in flight worker survives any number of armed collection wakeups while real budget failures and terminal uncollected results still escalate, and collection retires the reaping race by making a full file `Read` of the persisted `outputFile` the primary path with footer capture, async launch receipts stamping `startedAt` and the output file location, and an env gated raw response debug capture
- worker budgets fit the observed failures: trivial tier token budgets roughly double and the fast tier rises to 48000 output and 360000 uncached tokens, all still env overridable
- one call settlement accepts `--accept-failed-transport`, so reaped but verified deliveries settle in the strict `--record <id>=<verdict>` form without the raw args transport
- the codex jobs monitor terminalizes instead of announce only: dead pid running records and pidless cleanup required records past a grace reconcile through the companion's lock protected repair and announce the terminal outcome exactly once, with adversarial coverage for pid recycling, grace boundaries, unavailable identity, and clock skew
- the inline guard records enforcement and polices heartbeats: deny and warn audit events carry path, tool, counts, budget, and mode, informationless no op Bash commands are denied while fusion tasks are in flight (advisory mode warns), and the PreToolUse matcher routes Bash to the guard
- grok terminal records carry attribution: resolvedModel and resolvedEffort backfill from partial envelopes or session metadata on cancelled and died paths, the `grok-role` header becomes a structured record field, and 402 balance exhaustion appearing only in result text classifies as `quota` on failed runs
- the codex rescue transport ritual drops the read empty step (create, write, consume); consumption rejects unwritten transports on the execution path only, discard of an unwritten transport stays silent, and the fusion surface keeps empty argv as a valid default invocation
- fusion stats gains a fleet visibility section derived from guard audit dispatch bursts, and fusion config gains a codex live model listing drift audit reporting listing age, configured but absent ids, and gpt family newcomers, with a missing cache reported as unavailable without failing the run
- the rules codify the fleet: default fleet posture for any goal with three or more independent packages, single concern briefs, verification lists that include shared harness suites, peer sandbox findings reported with passing counts and rerun by the orchestrator, a checkpoint protocol for fleet waves over uncommitted baselines, and text only heartbeats on advisory wakeups
- test suites are environment isolated and deterministic: both companion harnesses build child env from allowlists with sandboxed `HOME` and `TMPDIR`, the fake CLIs bound their stdin drains and refuse real home fallbacks, module load `PATH` mutations are removed, transport and stats suites pin their data roots, monitor suites wait on conditions with posix guarded mode assertions, the leaderless group test proves direct leader death positively, startup window and silence assertions keep bounded holds, and a linux only reap diagnostic instruments the kill escalation for the node 24 variance

## 0.0.27

- model selection is a real flag, not prose: the routing rules require quick and volume dispatches to pass `--model` and `--effort` as leading companion options (gpt-5.6-terra and gpt-5.6-luna at xhigh), the peer brief header line is downgraded to audit prose, grok burst dispatches pass `--effort low`, and the codex companion warns with an additive `modelDrift` record field plus a footer line on every terminal render when a brief header names a model the job did not run
- codex threads keep their routing on resume: `--resume` and `--resume-last` inherit model, effort, and service tier from the thread's latest job unless explicitly overridden (marked `inheritedFromThread`), and every task and review spawn pins `-c service_tier=priority` by default with a `--service-tier <id|none>` override plus `serviceTier` and `appliedServiceTier` record fields
- the foreground timeout path fits the Bash cap: the timer arms after stdin delivery, rollout recovery and cumulative usage finalize before the timeout verdict, an incomplete cleanup stays nonterminal as `cleanup-required` with process evidence retained, and `finishedAt` is written once so later finalization passes can no longer restamp terminal times
- the Stop gate stops blocking on in flight work: still running owned tasks let the turn end with a one line armed collection notice and an `awaitingCollection` marker, only terminal uncollected results block (new `ready_uncollected` status), collected harness async deliveries clear `unexpected_async` into `deliveryMode: harness_async`, and TaskOutput collection backfills turns, tool calls, and usage from the task transcript
- verdict settlement is one call: `/fusion:stats --record <id>=<accepted|rejected|unverified>` accepts fusion task ids or engine job ids and writes the worker ledger and the linked engine record together via the collection captured `peerJobId`, strict record forms pass as direct arguments while free text still rides the raw-args transport, the grok companion gains a `record-acceptance` subcommand mirroring codex, and worker rows arm `awaitingVerdict` until settled
- codex setup probes recent job logs for models cache schema drift (the missing field match requires the `codex_models_manager` marker), and the two known load flaky suites (grok state lock concurrent reapers, codex exec timeout reaping) are stabilized with deterministic identity fakes, explicit synchronization, and load tolerant harness budgets without weakening any assertion

## 0.0.26

- the codex plugin moves to the skills surface: all nine legacy `commands/*.md` become `skills/<name>/SKILL.md` with byte identical bodies and unchanged `/codex:*` invocation names, matching current Agent Skills guidance that marks plugin `commands/` directories as legacy
- every skill carries open standard frontmatter: an explicit `name` matching its directory across all three plugins, an `argument-hint` for `/fusion:config`, and descriptions rewritten to the what plus when formula with trigger contexts aligned to the routing policy, while `panel` and `ultra` keep their test asserted description and `when_to_use`
- reaped async wrappers reach a terminal state: a `TaskOutput` or `TaskStop` no task error for a session owned non terminal worker record flips it to `failed` with the new `task_reaped` failure kind and `reaped` collection method, the Stop gate stops demanding collection for reaped records, and recording accepted on a failed worker transport requires the same `--accept-failed-transport` override as the codex path
- grok-prompting documents the wrapper dispatch grammar: companion options lead the raw request and are separated from the brief by a standalone `--`, so a live web consult begins with `--web --` and prose inside a brief never enables web tools

## 0.0.25

- grok turn limits are salvageable: the consult default rises from 25 to 60 turns, a max turns death is classified as the new `turn_limit` failure kind outside every breaker table, and the final json envelope printed before exit 1 is preserved so partial text, usage, model usage, and structured output survive into the job record
- codex acceptance closes the loop: a new `record-acceptance` subcommand writes semantic verdicts into the job record under state locking, recording accepted on a failed transport requires an explicit `--accept-failed-transport` override, `/fusion:stats --record-acceptance` routes through it with a ledger only fallback for older companions, and the stats report gains an acceptance anomalies block for verdicts that contradict transport status
- codex jobs carry their requested model and effort from creation so never finished jobs stop reporting null, task mode forwards `--skip-git-repo-check`, and the trusted directory startup refusal names that remedy
- monitor announcements are session owned: a completion is announced only by the Claude session that dispatched it while records without an owner keep cross session visibility, snapshot carry forward stops rewriting observedAt, and the model audit sidecar resumes appending for directly observed terminal jobs after previously being fed only by the legacy rollout path
- worker budgets match the observed workload: wall clock 480000 to 1200000ms, stall 180000 to 300000ms, uncached tokens 120000 to 240000, turns 40 to 60, every collection path stamps `collectedAt` idempotently, and Stop adds a non blocking advisory when collected workers remain acceptance unverified
- verified peer versions are recorded: `plugins/fusion/verified-versions.json` is the single source (codex-cli 0.144.4, grok 0.2.101), the readme prerequisites quote it, doctor check 17 compares installed cli versions against it, and a test keeps the readme and the json in sync
- docs: troubleshooting covers the turn limit family, the models cache schema fight between codex generations sharing one `CODEX_HOME`, acceptance integrity, and the raised worker budgets, with the grok and codex contracts updated to match

## 0.0.24

- raw request transport now keeps every slash command and wrapper agent argument out of Bash: companions mint one-time private staging files (session bound, owner only modes, one hour expiry, consumed before parsing) behind a Read verify Write handshake, Codex adds a `--request-stdin` ingress for programmatic orchestrators, Grok prompts travel through `/dev/stdin`, and a repository wide surface test enforces the handshake on every transport command
- Claude workers are now owned lifecycle jobs: dispatches require a minimal `fusion-brief: v1` envelope, background launches are denied without an explicit user `--background`, injected task ids correlate parallel workers, wall clock, stall, turn, and token budgets cancel runaways, Stop blocks until every async worker is collected or cancelled, and `/fusion:stats` reports worker lifecycle, exact usage, and `--record-worker-acceptance` judgments
- transport completion, semantic acceptance, and delivery are now independent dimensions on both peer engines; a done transport with unverified semantics renders an explicit warning instead of posing as a deliverable, and non foreground collection stamps `deliveryCollectedAt`
- Codex execution is contained and salvageable: every exec pins `--strict-config` and disables both multi agent generations with collaboration tool calls failing closed as `policy`, timeouts and cancellations recover partial output, cumulative usage, and the actually resolved model and effort from the local rollout without turning failures into successes, process identity settles before liveness verdicts, zombie processes count as dead, preflight rejects Codex CLI versions below the tested floor with a typed `setup` failure while never blocking newer releases, and `/codex:history` lists canonical jobs across workspaces with resumable thread ids
- Grok headless execution is source verified against the opened upstream: capabilities are probed before any invocation and missing flags exit as `setup` with zero calls, the child environment scrubs Claude and secret bearing variables while pinning memory, subagents, managed MCP fetching, and all eighteen compatibility bridges off, the prompt is withheld until a matching `ProfileApplied` sandbox event proves strict enforcement, a clean exit without positive tool allowlist evidence fails closed as `policy`, stdout spools to an unlinked private file with a 32MB cap, and the usage envelope is validated for arithmetic consistency with authoritative integer cost ticks
- Grok reviews are now one structured call: `--json-schema` output replaces the corrective retry and spend merge machinery, and review verdicts are trusted only from the schema channel with text parsing kept solely as an absence fallback
- `/grok:history`, session continuity, and opt in memory land together: history lists canonical job metadata and resumable sessions without reading briefs, results, or native session contents, continuity defaults to manual with an optional `claude-session` affinity that resumes only a compatible completed task from the same session, working directory, mode, and memory boundary, Fusion routed briefs always start fresh, `--fresh` opts out for one task, and `--memory` is an explicit opt in that review, stop gate, and best-of-n never inherit
- detached collection is a closed loop: the collector accepts exactly one engine and one job id, binds the returned terminal marker to that dispatch identity, reports state and semantic status in a structured line, and Stop refuses to finish until failed collections are reported as uncollected with their result command and every collected peer transport receives an explicit accepted, rejected, or unverified judgment through the engine matched stats command
- the circuit breaker distinguishes immediate hard failures (quota, auth, missing CLI, protocol, transport, sandbox, setup) from repetition gated ones (rate limits, timeouts, stalls, process deaths), splits ordinary tool permission denials from transport initialization denials, includes Claude worker evidence, and recommends rerouting to a healthy lane; the inline delegation guard now enforces its write budget by default with an advisory compatibility mode
- stats aggregation fails closed on token overflow instead of reporting wrong totals, records resolved model and effort dimensions on both engines, adds Grok headless turn and cost coverage, and scopes Codex acceptance reconciliation to the recording session with an idempotent ledger
- documentation now matches the verified upstream: compatibility bridge and subagent wording corrected, ACP described as available today with per workspace pooling guidance, permission mode claims narrowed to what upstream wires, the Linux bubblewrap refusal versus macOS asymmetry and the unbounded sandbox event log documented, the headless envelope casing described exactly, and the doctor audits native Grok configuration exposure through `grok inspect --json`
- housekeeping: the shared transport staging prefix is engine neutral, a dead PreToolUse guard hook registration is removed, permission rule content rejects commas that upstream splitting would break, `--no-auto-update` is capability gated, the suite timeout rises to 120 seconds, and the bench CLI probe window widens to five seconds

## 0.0.23

- role ownership is now explicit across rules, skills, agents, and docs: the main Claude session is the sole control plane and final judge, Claude workers are native specialists, Codex owns ordinary implementation and default deep external review, and Grok keeps five protected specialist and burst roles
- Grok consult now requests strict operating system sandbox read scoping, resets inherited approval bypass, exposes a hard file and optional web read tool set, denies shell, edits, MCP, and subagents, terminates fresh process groups that report failed sandbox enforcement even when persistent identity capture is unavailable, rejects cross mode or legacy unknown profile resumes, and documents the platform sandbox and trusted hook boundaries
- Grok background handling now separates Agent scheduling, companion delivery, and CLI process supervision; explicit incoming background requests create manual receipts, while managed long runs stay inside their owning Agent, preserve text or JSON terminal envelopes, mark collection only after output, return an explicit nonterminal cleanup-required failure instead of looping, and receive a delayed cross workspace monitor fallback after the grace period when terminal output remains uncollected; worker and CLI launches atomically gate cancellation and publish process ownership, monitor output is synchronously acknowledged before deduplication, and partial or malformed workspace snapshots cannot erase or mute notification state
- Grok jobs now use 128-bit identifiers with exclusive record and brief creation, reject ambiguous legacy ids, and atomically lease resumed sessions so concurrent continuations cannot mutate the same Grok thread
- Codex is now a first party companion in this marketplace with no `codex-plugin-cc` dependency; it drives the official `codex exec --json` protocol directly, persists durable job and event state, supervises the owned process group, supports native and focused review, preserves resumable threads, and records exact token usage only when the protocol makes it available
- Codex forwarding now stays attached in both the rescue Agent and companion Bash call regardless of complexity, duration, review size, or model; only an incoming request with explicit `--background` may detach, Fusion gives receipts from its own orchestration one bounded collection attempt in the same turn, and direct slash command users inspect through status and collect through result
- detached Codex startup now uses a durable two phase worker approval before Codex can spawn; failed launch probes record abort first, post approval failures leave the verified worker in control, and incomplete process identity checkpoints retain cleanup evidence instead of creating a false terminal result
- Fusion background collection now uses a 540000ms window inside a foreground Bash call with a 600000ms tool timeout; longer jobs return an explicit uncollected timeout receipt instead of relying on the previous 2400000ms cap that the tool could never reach
- the Fusion session breaker now preserves one retry after an isolated peer rate limit and opens only after a second rate limited outcome from the same peer inside the configured lookback without an intervening successful job
- the hosted Codex task surface adds explicit `--web`, `--write --network`, `--fresh`, model, and effort controls while forcing web search and workspace network access off by default and never exposing danger full access; Fusion routing, doctor, config, benchmark profiles, setup guidance, and shared companion documentation now target `codex@claude-code-fusion`
- Grok now classifies HTTP 402, Payment Required, exhausted balance, insufficient account balance, credit, credits, or funds, and usage limit failures as quota, while stats and the session breaker recover the same classification from legacy failed record summaries or bounded job logs without mutating terminal history
- Grok job records now retain CLI reported usage, per model usage, and the 0.2.101 `usage_is_incomplete` signal on successful and failed runs; Grok plus fusion stats aggregate token totals only from complete reports, and corrective retries cannot synthesize exact usage from partial attempts or hide a real second attempt failure behind review validation
- Grok record locks now bind ownership to boot, process start, and command identity; token conditional release, serialized dead owner recovery, dead reaper succession, fail closed corrupt ownership, and prepared or released owner scavenging prevent PID reuse wedges and concurrent writer races
- Codex stats now scope linked worktrees by shared Git repository identity, retain terminal monitor evidence after disposable state cleanup, recover actual model, effort, and exact per turn token usage from rollout records, and keep unavailable history honest
- Codex transport completion and semantic acceptance are now separate dimensions; terminal jobs default to unverified until collection verification records accepted or rejected
- Fusion's Codex monitor and ordinary stats observation paths now leave process signalling plus terminal repair to the owning companion, trust the adapter's stored repository identity after worktree removal, and use exact Claude session identity for new trace records with a legacy time fallback
- the inline guard now appends a sanitized, rotating, long lived JSONL audit of main loop writes and Agent or Task dispatches, fusion stats can aggregate or recover session traces from that history, and each dispatch starts a fresh advisory window so later inline accumulation is no longer permanently silenced
- Grok process signalling now verifies recorded process identity before any signal, never targets PID 1, restricts records without identity to graceful cleanup, keeps unverifiable terminations running with a cleanup marker instead of a false terminal state, and resume-last no longer crosses Claude sessions when a session id is available
- Grok and Codex data directory overrides now require absolute paths and Fusion ignores non absolute state root overrides instead of resolving them against the current directory
- the shared contract now lists protocol and process as Codex adapter extensions, and dead Codex result file plumbing, an unreachable worker state branch, a dead status render fallback, and a no op prune scan call were removed
- Codex termination now interrupts gracefully with SIGINT before SIGTERM and SIGKILL because the Codex CLI handles only SIGINT, pre-turn configuration and deprecation warning items no longer fail runs as protocol errors, setup recognizes CODEX_API_KEY environment authentication that login status cannot see, and the Fusion monitor resolves rollouts under CODEX_HOME
- Grok background launches print a delivery line so forwarders chain managed collection mechanically, the jobs monitor namespaces announcements by workspace and disambiguates duplicate legacy ids with --cwd, and regression tests pin the unavailable identity probe resume path and the ignore-term escalation race

## 0.0.22

- codex worktree observability now scans worktree workspaces, uses mirror aware died detection, deduplicates model audit observations across processes, and reports model@effort stats without double counting terminal copies
- grok companion hardening reaps stale locks, terminates orphaned processes on the died path, tolerates individual malformed monitor records, records malformed reviews as errors, enforces the footer contract, includes errorMessage on every failure path, scopes lookups with --cwd, and covers resume-last selection
- background job collection now lives in plugins/fusion/scripts/job-collect.mjs, with fusion:job-collector reduced to a thin forwarder
- SessionStart now runs a peer lane breaker check, while the inline delegation guard emits an actionable advisory
- CI JSON validation gains empty glob guards so unmatched patterns do not become literal paths
- documentation and routing rules now match install troubleshooting reality, xhigh ceiling wording, the consult allow list, split failure taxonomy, stop gate semantics, README uninstall steps and environment variables, doctor consistency checks, codex volume lane adoption nudges, and orchestrator owned worktree lifetime
- rules manifest generation now hashes only content reachable from the current release lineage plus the current working tree, so unreleased side branch states are not treated as shipped versions
- the inline guard now records a per-session dispatch ledger, while fusion-stats --trace joins dispatches with exact grok and approximate codex job outcomes

## 0.0.21

- quick scoped packages now default to gpt-5.6-terra with effort xhigh named explicitly, while same turn grok overflow names low effort or the account's fast coding SKU and retains the max turns guard; motivated by the stated user preference on 2026-07-12 following the 0.0.20 release watch, which measured grok at the overwhelming share of delegated volume
- trivial packages, drafts, review comment triage, and research digest backup now default to gpt-5.6-luna with effort xhigh named explicitly, while grok at low effort handles overflow, grok path batch codemods may explicitly pilot grok-composer-2.5-fast, and fusion:trivial-worker remains the tool surface or dual engine fallback; motivated by the same stated preference for the OpenAI 5.6 family and the need to keep light work on an explicitly named tier instead of an implicit flagship
- the codex lane remains single flight per workspace and sends overflow in the same turn instead of queueing, to an isolated worktree when the package is worktree eligible and to grok otherwise; retained deliberately because the codex plugin's upstream job state is not concurrency safe, which is also why overflow exists
- grok keeps five reserved seats for same turn overflow, independent second opinions and the panel cross check, live web research consults, very large context reads, and best of n tournaments, while /grok:review remains on grok and sol at ultra spec grade routing stays unchanged; motivated by preserving cross family review, live web access, large context capacity, and tournament diversity after the volume preference inversion
- fusion:job-collector now moves from haiku to sonnet and its contract externalizes elapsed time accounting, adds terminal marker fallback words, escapes dead processes while capturing the job log tail, and enforces an absolute cap; motivated by telemetry across twenty recent collector runs showing about a quarter malfunctioning through narration finals, a fifty minute run past its own cap, and silent stalls on dead jobs
- codex-jobs-monitor now captures model and effort from the live process argv for running jobs whose records lack them, appending one observation per job to an append-only model-audit sidecar under fusion's own data directory, while fusion-stats resolves each codex job's model request first, then sidecar, and reports a byModel breakdown with unknown counted explicitly; motivated by the codex record layer persisting request.model and request.effort unreliably, which left no audit after the fact of what a job actually ran
- quick scoped and volume routing gain a worktree parallel ladder: when the codex lane is occupied and a package is worktree eligible (disjoint files, no heavy dependency install for verification), the codex rescue dispatches inside an isolated git worktree so the companion sees a distinct workspace and single flight does not collide, with the diff merged back and re-verified in the main tree as part of collection and /fusion:stats --prune-dead cleaning the disposable state directories; motivated by the per-workspace single flight guard otherwise serializing the preferred codex volume lane while its upstream store stays concurrency unsafe
- the npm test script gains a 30 second per-test timeout via `--test-timeout=30000` so an intermittently hanging test fails fast by name instead of wedging the runner; motivated by four observed full-suite hangs whose culprit was never named
- the readme layer table and quick start track the 0.0.21 routing: terra and luna defaults with grok's reserved seats

## 0.0.20

- fusion:deep-reasoner moves from opus to fable at unchanged xhigh effort; opus holds no standing routing seat and serves only as the fallback orchestrator when fable is unavailable
- the capability table gains gpt-5.6-luna (fast and affordable codex volume tier) and grok-composer-2.5-fast (unbenched codemod pilot) with provisional scores to re-score after the first real jobs; quick scoped codex briefs name terra and high volume light work (drafts, review comment triage, digest backup) names luna
- the shipped rules template carries the scored model table between the fusion:model-table sentinels instead of the unscored placeholder, so a fresh install reads concrete rows while /fusion:config regeneration still owns the block
- panel adjudication is weighted instead of equal votes: the codex verdict from sol at ultra leads on long horizon correctness and architecture, the grok verdict at xhigh is an independent cross check on large context facts and operational pragmatics, verdicts are never averaged, and disagreement triggers exactly one targeted follow up on the disputed point
- general-purpose and the built in Plan agent are retired from routine routing after nine days of telemetry showed them dominated on latency (general-purpose median 9.4 minutes over 167 dispatches vs Explore at 2.9 and grok consult at about 2); search goes to Explore, pinnable to haiku for simple sweeps, research goes to grok consult, and routine planning stays in the main loop
- fast-worker gains an execution speed doctrine (single script rule, batched independent tool calls, extraction reads, lean output, verify in run), motivated by a mining package that spent 53 tool round trips and 12.5 minutes on commands totaling 19 seconds; it is also the required lane for privacy bound inputs that must not reach peer engines
- spec grade codex routing encodes the foreground cap: a synchronous companion call dies at the Bash tool's 10 minute limit while sol at ultra jobs average about 11 minutes, so packages likely to exceed the cap run backgrounded with same turn fusion:job-collector collection and forcing foreground on such a package is a routing error; motivated by four codex jobs in one day killed mid run with their work complete but their reports lost
- quick scoped grok routing gains a max turns guard: a long verification chain consolidates to a spec grade codex brief or carries an explicit --max-turns override, and a max turns death is a routing error, not a retry candidate; motivated by a 15.7 minute write job dying at the 60 turn budget
- troubleshooting: hitting max turns on the first attempt marks the package too big for the lane (split or reroute, never retry the same scope), and a repeated /grok:review permission death on the same diff routes to /codex:adversarial-review or fusion:deep-reasoner while the grok CLI omits the denied call from its output
- three orchestrator side dispatch disciplines land in the delegation rules: briefs arrive pre staged with exact paths plus a sample record for data work, independent packages dispatch in a single message, and related follow ups reuse the existing worker thread via SendMessage
- the readme layer table tracks deep-reasoner's move to fable

## 0.0.19

- gpt 5.6 era refresh: the capability table gains gpt-5.6-sol and gpt-5.6-terra rows next to gpt-5.5 (which stays the codex CLI coded default), and the quick scoped and trivial routing rows name the codex terra class fast tier as the explicit alternative when grok is unavailable or codex is preferred
- routing rules state that the codex CLI coded default can trail the newest generation while user config pins newer, so live listings and /fusion:config stay authoritative; peer implementation packages must verify tests were not deleted, skipped, or weakened to pass
- /fusion:doctor and /fusion:config enumerate the codex catalog live via codex debug models --bundled and ~/.codex/models_cache.json, mirroring grok models; doctor also flags when the installed codex plugin wrapper accepts a narrower effort set than the CLI catalog, and the hardcoded effort list is gone
- grok consult mode allowlists WebSearch and WebFetch when the brief requests --web, so read only research digests no longer die on the permission gate; a permission failure on a --web consult now reads as an allow list regression in troubleshooting
- /fusion:ultra routes facets by peer strengths instead of the retired implementation versus quick turnaround split and no longer names TodoWrite
- bench run records capture peer default models (peerDefaults) so cross generation results stay comparable
- the readme peer row points at the /fusion:config capability table instead of naming model generations, and the rules sync warning names the stale render case and says the scored model table is preserved
- background job monitors announce only detached background jobs on the grok side, deduplicate announcements across restarts on both engines, and exit clean on EPIPE; the recurring exit 144 crash traced to an external signal kill, so restarts are now idempotent
- the inline delegation guard no longer denies anything: it counts main loop writes and agent dispatches per session and emits at most one advisory line per budget multiple when writing runs ahead of delegation; the allow escape hatch is retired
- /fusion:stats gains --session (current session lane counts from the guard state plus per engine job counts) and --prune-dead (dry run listing of workspace state dirs whose recorded cwds no longer exist; --yes deletes)
- peer briefs open with a header line naming the lane, the effort or model tier when the routing row requires one, and the verification command; review shaped work (PR and issue verification, comment triage, reproducing reported bugs) gets a routing row to /grok:review and /codex:adversarial-review
- troubleshooting gains a session limit failure row (preserve the brief verbatim for re-dispatch after reset) and a collection repair recipe for receipt or invisible finals; grok-rescue's final message contract forbids referencing invisible content
- /fusion:setup records reconcile decisions per template hash in reconcile-decisions.json so the same overwrite question is never asked twice
- CONTRIBUTING.md documents the rules release checklist with the load bearing invariants and the 0.0.15 lesson
- grok consult mode also allowlists the read only test runners node --test and npm test, so reviews of script heavy diffs can verify claims instead of dying on the permission gate
- the monitor and guard changes went through an adversarial codex review; all ten findings are fixed fail closed: pruning treats unreadable records, missing or relative cwds, and scan to delete races as unsafe, guard state updates serialize under a lock, codex dedup namespaces per workspace, and monitors survive transient snapshot loss and announce the restart backlog
- the consult permission gate names the blocked tool call when the CLI reports it and says so explicitly when it does not, because the grok CLI currently omits the denied call from every stream the companion can see

## 0.0.18

- the routing rules upgrade grok from the fast coding lane to a second frontier implementation lane after xAI shipped its flagship as the CLI default on 2026-07-08; quick and trivial briefs now name low effort or the fast SKU explicitly so speed is intentional rather than assumed, and the design decision exclusion becomes a capability table condition instead of a baked weakness claim
- /fusion:doctor's drift check for the grok default becomes direction neutral against the capability table instead of assuming a fast tier
- hardcoded model names and context windows are removed from grok-prompting and the README (retired Composer references)
- the contract and skill docs drop version pinned verification notes and dated qualifiers; docs state the current contract only and get reverified after CLI updates
- /fusion:config gains a proposal flow that enumerates the live engine lineup, drafts a full scorecard with rationale (verifying against live sources when a default model is newer than the assistant's knowledge), confirms everything in one batched question, and writes only confirmed rows through the existing rescore plumbing, motivated by the capability table shipping empty by design while scoring stayed a chore nobody performed, so routing ran on qualitative prose indefinitely
- /fusion:doctor recommends the proposal flow when the table is entirely unscored and /fusion:setup points at it while the live rules still carry the placeholder table
- the grok companion gains blocking collection with `result --wait`, including the bounded `state: running` chaining contract, and `review --background` now rides a detached `review-worker` so review jobs have the same worker backed collection path as tasks
- grok-rescue and grok-review-runner replace `run_in_background` with foreground Bash calls using an explicit 600000ms timeout; long runs launch the companion with `--background` only when they chain `result --wait` to a terminal state in the same turn, and receipt final messages are banned, motivated by live sessions where every grok-rescue dispatch returned a started receipt instead of a deliverable and the orchestrator had to spawn a collector per package
- the codex lane becomes explicitly single flight (one package in flight per workspace, overflow spec grade work routes to grok) and gains a consolidation heuristic (three or more quick scoped packages on one subsystem with long horizon shape merge into one spec grade codex brief), motivated by usage forensics showing the 0.0.15 collection mandate plus frictionless grok defaults cut codex CLI sessions from 27 to 5 in a day while concurrent dispatches died on single flight rejections
- both plugins and the marketplace bump to 0.0.18

## 0.0.17

- the routing rules gain a session execution posture: coordinate, implement, and triage persist per goal across turns instead of reclassifying every message, a micro step gate bounds the main loop to one small directly verified edit per goal, and four accumulation triggers (two consecutive edit turns, five edits since the last verified collection, a second fix of the same file or symptom, a failed inline verification) force a declared transition to implement before the next product edit; motivated by measured sessions after 0.0.15 where interactive debugging classified every turn as a small step and accumulated 80 plus inline edits with zero delegation while package shaped work still routed normally
- delegation pressure returns in collection safe form: bias to fan out once the brief is verifiable, a balance check routing the next fitting package to an idle peer lane after two Claude worker dispatches, and an explicit statement that collection and verification are completion conditions, not reasons to keep executable work in the main loop; the dispatch gate now reads posture and goal continuation before the question versus change classification, and interactive design iteration is carved out so taste turns stay in the main loop while the mechanical spread is dispatched
- new inline delegation guard (plugins/fusion/scripts/inline-delegation-guard.mjs, wired as a PreToolUse hook): main loop writes inside the session cwd consume a budget of five (FUSION_INLINE_WRITE_BUDGET overrides), the next write past budget is denied with a routing checkpoint message naming an auditable allow command, Agent and Task dispatches reset the budget while Skill invocations deliberately do not (the allow command is the escape hatch for skill owned write flows), subagent payloads and malformed input fail open, and state is written atomically and pruned after 48 hours; the compact hook now asks the session to restate its posture after compaction
- test suite grows from 146 to 158 with a dedicated guard suite (budget boundary, resets, fail open paths, override parsing, pruning)

## 0.0.16

- new benchmark condition B3, the advisor profile: fusion enabled with claude tiers only like B1, but the main session model pinned to sonnet, testing the executor centric topology where a cheap model runs every turn and consults stronger tiers on demand; new claim C1c compares B3 against B1 claude billed tokens under the same comparability gate as C1a, so the official advisor pattern is measured against the default orchestrator profile instead of taken on faith
- the runner observes the profile instead of trusting the flag: conditionNotes gains a required mainSessionModel field read from the profile's settings.json (null when absent), and a B3 invocation refuses to start when the detected model is not sonnet, so mislabeled runs cannot contaminate C1c after the fact
- zero delegation handling splits by condition: B1 and B2 runs that never delegate stay invalid for claims, while a zero delegation B3 run remains claim eligible and is disclosed as valid-zero-delegation in the delegation table, since keeping work in the executor loop is the behavior under test
- test suite grows from 144 to 146: B3 acceptance and refusal paths, mainSessionModel recording, C1c rendering, and the zero delegation label

## 0.0.15

- the routing rules are rewritten around one output invariant, done means collected and verified, and come out roughly half the length; the codex row now requires synchronous execution inside every codex brief, same turn collection through fusion:job-collector naming the codex companion's status and result commands, a heartbeat rule against silent polling turns, and receipts and truncation merged into one non deliverable rule; escalation ladders and the circuit breaker table move out to a new plugins/fusion/rules/troubleshooting.md so the live rules stay lean while the reference material is still one read away
- new session scoped codex jobs monitor (plugins/fusion/scripts/codex-jobs-monitor.mjs, wired through plugins/fusion/monitors/monitors.json): jobs are scoped to the current workspace via the git root, failed and cancelled lines gain an errorMessage suffix, and a running record whose driving process has vanished is reported once as died instead of forever
- fusion:job-collector's contract narrows to foreground only polling; its final message is always the collected result or a collector timeout line, never a background handoff
- grok-rescue and the grok cli runtime skill stop passing --background on their own initiative; it is reserved for briefs that are explicitly watch style, with a documented recovery procedure for a stray flag
- test suite grows to 144 with a dedicated suite for the codex jobs monitor (session scoping, error suffixes, died detection)

## 0.0.14

- the background job monitor is session scoped: each session's monitor now emits only jobs whose recorded claude session id matches its own CLAUDE_CODE_SESSION_ID (verified injected into monitor processes), fixing live cross session noise where every session in a workspace heard every other session's completions; it degrades to workspace wide reporting when the env var is unavailable, absorbs pre existing terminal jobs regardless of owner, survives transient registry races (the per tick scan is exception proof, since claude code never restarts a crashed monitor), handles SIGHUP, and its line no longer tells the orchestrator to reflexively run /grok:result, motivated by a live monitor death (exit 144) and a user hitting the cross session leak within a day of 0.0.13
- collection is itself delegated: new fusion:job-collector agent (haiku, one detached bash loop polling a supplied status command until a terminal marker, then returning the result command output verbatim, with a collector timeout line on cap); the routing rules route codex background collection and timeout reruns through it, ban the main loop from touching companion state for any job a rescue agent owns, and mark session monitor lines about agent owned jobs as informational, motivated by live sessions with 43 main loop polls including a hand written 60 iteration codex loop
- routing rules additions: parallel packages must declare each other's files as intended in flight changes and never revert or clean anything outside their own list (a worker treated a sibling package's uncommitted work as drift and destroyed it), and the user facing quality floor now states that generated content deliverables are verified by reading the actual produced artifact, not by a passing verification command alone, motivated by a live session where a deployed fix still produced an off topic llm reply
- test suite grows from 127 to 130: session scoped emission (matching, mismatching, env unset) and the vanished jobs directory resilience case

## 0.0.13

- question policy: the routing rules gain a whitelist for what may reach the user as a question (product and design tradeoffs, hard to reverse actions, decisions the user reserved); execution mechanics questions are banned outright, motivated by live sessions where /grok:review interrupted every run with "background or wait?" and every review ended with "which findings should be fixed?"; review findings are now triaged by verification instead (confirmed ones dispatched for fixing without a prompt, false positives dropped with a stated reason, one fixed/dropped/why report), and surviving questions batch into one prompt
- every dispatch is a background subagent: /grok:review rides a new grok-review-runner agent (haiku, single background bash call, 30 minute companion timeout), /grok:task and /grok:best-of-n forward through grok-rescue, and grok-rescue itself launches the companion with run_in_background plus the raised timeout so long runs stop dying at the foreground bash cap; detached --background jobs are reserved for watch style work and explicit user requests; the escalation ladder now diagnoses the brief against the dispatch gate before blaming the engine, since a starved worker looks exactly like a weak one
- commands migrate to the skills layout: all 14 slash commands move from the legacy commands/ directories to skills/<name>/SKILL.md with invocation names unchanged; panel and ultra split their long descriptions into description plus when_to_use, trimming the skill listing budget
- stop gate becomes a plugin setting: plugin.json declares a stop_gate userConfig boolean, the companion honors CLAUDE_PLUGIN_OPTION_STOP_GATE with precedence over the legacy flag (true/1 enables, false/0 disables, garbage and unset fall back), and /grok:setup keeps the CLI flags as the scripting fallback
- background job monitor: a monitors/monitors.json entry runs jobs-monitor.mjs for the session, which absorbs pre existing terminal jobs silently and then emits one line per job newly reaching done, error, or cancelled with the failure kind and a /grok:result hint; interactive sessions now hear about detached job completions without polling, while the companion contract keeps collection via status and result as the reliable path
- marketplace and manifest modernization from a full docs audit: top level description and version replace the metadata block, both manifests gain $schema, displayName, homepage, repository, keywords (plus category and tags per marketplace entry), ci adds claude plugin validate --strict, the readme floor rises to claude code 2.1.198 (the dispatch flow relies on background subagents being the default), forwarder agents declare background: true, and the stop gate hook gains a statusMessage
- test suite grows from 118 to 127: stop gate env precedence (true, false, garbage fallback, unset) and the jobs monitor (no history replay, single line per transition, failure kind reporting, sigterm exit, missing directory tolerance)

## 0.0.12

- dead process detection in the grok companion: status, result, and cancel verify the driving process before trusting a running record; a dead process is persisted as failure kind died with the check re-run under the record lock (a racing completion or pid write survives, found by cross engine review), a record with no recorded pids gets a launcher grace window (default 15000ms, GROK_COMPANION_PIDLESS_RUNNING_GRACE_MS) after which it is treated as died, EPERM counts as alive, and terminal records are now fully immutable; motivated by a live session where a peer background job died silently twice while its status reported running for 20 minutes
- routing policy generalizes the incident into engine agnostic principles: a running status from any background executor is a claim to verify rather than trust (the grok companion now checks itself; codex is checked via ps and its rollout file), died joins the circuit breaker table (cancel, one resume with the failure attached, escalate to a different executor on a second silent death), and rotation discipline extends to every warm peer thread including codex resume since oversized threads are the ones that die mid stream
- docs/companion-contract.md becomes the single canonical companion contract (job lifecycle, liveness, rendered outcome lines, failure kinds, background collection, resume-last selection, stop gate verdicts); docs/grok-contract.md is demoted to the grok instance keeping only CLI specific facts, and the readme drops the hardcoded two peer world view for an open engine set with grok and codex as current instances
- generality batch from an eight facet fleet audit: nested CLI deny rules generate from one exported name list, fusion stats gains a provider registry and file engine descriptors so an engine section is one data entry, ci json validation derives from globs with empty glob guards, the bench codex version probe reads the real plugin cache instead of a nonexistent path, bench redaction becomes a data driven rule registry, worker agent frontmatter stops naming peer engines so routing has one source of truth, and /grok:stats and /fusion:stats agree on the mean wall clock label
- the audit's over generalization verdicts are recorded as deliberate non changes: no shared companion core, no cross plugin doctrine imports, and no n condition bench schema until a second hosted engine or third measured condition exists, keeping marketplace plugins self contained
- test suite grows from 103 to 118 and the companion test scaffolding dedupes into tests/lib/companion-harness.mjs and tests/lib/git-fixture.mjs with per file behavior preserved (71 tests before and after across the nine refactored files)

## 0.0.11

- grok companion contract hardening: cancelled jobs now persist status cancelled and emit state: cancelled with failure: cancelled everywhere (previously failJob wrote state: error even for cancellations, breaking the parseable outcome contract); quota and rate limit patterns match before auth so mixed stderr like "quota exceeded, login required" classifies as quota; a zero exit run with a malformed json envelope is recorded as failure: error with a bounded stdout tail instead of done; pre run errors (bad --prompt-file, bad --cwd) emit parseable state and failure lines too
- job state is now crash and race safe: records write through temp file plus rename under a lock, terminal states never regress, and a success that lands after a cancel was requested finishes cancelled instead of done (locked check and set on cancelRequestedAt, found by cross engine review); cancellation and SessionEnd cleanup escalate SIGTERM to SIGKILL with exit verification instead of fire and forget
- consult mode is hardened and documented: the allow list now denies shell chaining, redirection, backticks, and dollar expansion (the installed grok CLI matches bash rules by prefix, so wildcard allows were bypassable with compound commands); consult briefs must instruct file reads only, written into grok-prompting and the rescue agent so orchestrators stop learning it from failure: permission at runtime
- routing policy learns from a live fleet session: codex background jobs are collected by the main loop (/codex:status, /codex:result), a forwarder agent returning only a "task started" line is not a truncated run and resuming it is futile by contract, and the escalation section gains quality driven escalation (a completed run can still fail review; correct but substandard output re-dispatches one rung up with critique attached, judging the output not the price tag)
- orchestration.md restructured for llm compliance: implementation routing and failure kinds become tables, dispatch becomes a numbered procedure, routing gains an explicit priority order (correctness and safety, then user facing quality, then latency, then cost) and a user facing quality floor (ui, copy, api design, docs, and naming need a taste review before acceptance); subscription economics framing replaced with availability aware language, model names moved out of load bearing sentences
- user scoreable engine capability table: /fusion:config now edits ~/.claude/plugins/data/fusion-claude-code-fusion/model-routing.json (intelligence, taste, cost, 1 to 5 per engine) via the new fusion-config.mjs, and rules sync renders the scores into a sentinel region of the live rules; drift detection becomes template aware (manifest format 2 hashes with the generated region normalized) so scored live rules are never mistaken for hand edits, an invalid routing file fails closed keeping the current live table, and /fusion:doctor and /fusion:setup compare template hashes instead of raw bytes
- agent descriptions align with routing: trivial-worker and fast-worker describe themselves as fallback and tool surface lanes so auto delegation cannot prefer them over the grok and codex primary lanes; /fusion:stats is documented as peer delegation history (not the full engine mix); doctor reports effort and maxTurns alongside model and checks the codex job state directory
- bench harness can no longer publish misleading numbers: failed runs write records instead of disappearing, env.json captures plugin and cli versions, the task manifest hashes every file in a task (fixtures, mutants, hidden verifiers) instead of just brief and verify, B1 setup actually excludes peer plugins, summarize computes the claimed comparisons (C1a, C1b, C2) with billed token totals and prints not measurable where data is missing, zero delegation B runs are flagged invalid for claims, and the publication gate requires the full 8 to 12 task suite
- test suite grows from 60 to 103: quota and generic error failure kinds, validateReviewOutput and parseArgs units, fusion-stats with a live grok path, model table rendering and drift, the cancel race, stop gate first line parsing, and bounded output; ci adds macos, json manifest validation, and bench manifest freshness (node bench/manifest.mjs --check)
- docs catch up: readme lists /fusion:ultra and /fusion:config in the layout, GROK_CONSULT_ALLOW joins the documented overrides, SECURITY.md states that briefs are not redacted and secrets must never enter peer briefs, the unused review-output.schema.json is deleted in favor of validateReviewOutput as the canonical review contract, and grok-cli-runtime documents the real flag surface

## 0.0.10

- model aware peer lanes: codex is now the primary implementation lane (the current GPT 5.x flagship, spec grade briefs with explicit completion criteria, output contract, boundaries, and a verification command); grok is the quick turnaround lane (Composer 2.5 Fast by default) for small fixes, drafts, research digests, and large context reads; trivial single file tasks now default to grok, with fusion:trivial-worker as the fallback
- new /fusion:config command reads local model configuration across engines, enumerates available models, and changes defaults interactively
- /fusion:ultra facet routing: codex takes implementation and adversarial facets with spec grade briefs; grok takes high volume facets, research digests, and scoped fixes
- /fusion:doctor gains a peer engine defaults check (model keys from each CLI config, never the full files) and points at /fusion:config and `grok models` for remediation
- the codex prerequisite is explicit: codex@openai-codex powers the implementation lane, the rules document per call model and effort overrides (including the spark alias) and the fallback routing when the plugin is absent, and /codex:adversarial-review becomes the named cross engine review command

## 0.0.9

- rules sync is automatic: a SessionStart hook compares the installed ~/.claude/rules/orchestration.md against the plugin's canonical copy and a manifest of every released version's hash; a missing file is installed, a file matching any released hash is safely updated (provably never hand edited), and unknown content is never touched, with a one line notice pointing at /fusion:setup to reconcile. /fusion:setup narrows to first install permission checks and local edit reconciliation, and a guard test fails the suite when the rules change without regenerating the manifest
- routing policy gains the pre dispatch side: classify the user's message before any dispatch (questions and problem descriptions are handled in the main loop with read only tools, executors go out only for actual change requests); reconnaissance and execution are two dispatches on unfamiliar surfaces; and the verification command becomes the dispatch gate, with the main loop owning what to do and how to verify while the worker owns how to implement

## 0.0.8

- routing policy: worker reuse is scoped and rotated. a follow up goes to the same worker only when it touches the same files or surface as its previous brief; new packages, different surfaces, and parallelizable work get fresh agents; after about three follow up resumes or when notifications show usage nearing the context window, rotate to a fresh worker whose brief carries the accumulated decisions (observed live: one ui worker reached 78 percent of its window after five follow up resumes). truncation resumes count toward rotation and the same discipline applies to grok --resume-last threads

## 0.0.7

- worker turn caps are runaway breakers, not task size estimates: fusion:fast-worker goes from 30 to 120 turns and fusion:deep-reasoner from 40 to 80, after four fast-worker packages in one day exhausted the 30 turn cap mid implementation and their dangling progress narration was reported as the final result; fusion:trivial-worker stays at 15 by design, since exhausting it means the task was routed wrong
- routing policy: truncation is not completion. a worker result that ends in forward looking narration or lacks the verification output its brief demanded is treated as a truncated run and resumed with SendMessage to finish and re-report, instead of being read as a final report
- public benchmark scaffolding: bench/METHODOLOGY.md defines the claims under test (C1a Claude tier quota displacement, C1b peer offload increment, C2 wall clock compression, C3 typed failure surfacing scoped to the tested grok contract) with condition isolation, pre registered task manifests, verifier isolation with mutant self tests, a fixed exclusion enum, and a publication gate; the draft went through a blind two engine adversarial panel and every blocking finding was folded in
- the harness ships alongside it: run.mjs (isolated CLAUDE_CONFIG_DIR per condition, verifiers run outside the model visible worktree, token accounting by model and role from transcripts), a strict run record schema, summarize.mjs (pass matrix, median and IQR, refuses to compare snapshots from different task manifests), manifest.mjs (content hashed task registration), redact.mjs (path and credential scrubbing), and the first task T01-seeded-bug with a mutant self test; 12 new tests, suite at 60
- no results are published yet and the README says so: the task pool holds 1 of the planned 8 to 12 tasks, so no snapshot passes the methodology's own publication gate; in tree results stay lean (runs.jsonl, env.json, summary) with full redacted transcripts attached to releases instead of committed to the tree

## 0.0.6

- permission failure kind: a consult mode run whose tool call fell outside the allow list was reported as done with empty or partial output, because the grok CLI cancels the whole turn and exits 0 with stopReason Cancelled; the companion now detects that shape across the foreground, background, review, and stop gate paths and converts it to state: error, failure: permission with guidance to re-dispatch with --write or rewrite the brief (observed live: research briefs calling gh api died silently twice in a row)
- consult mode allow list gains read only gh subcommands (pr view/list/diff/checks, issue view/list, repo view, search, run view/list, release view/list); gh api stays write mode only since it can send mutating requests with the user's credentials
- GROK_CONSULT_ALLOW appends comma separated extra allow rules to consult mode per machine; deny rules still beat them and write mode ignores the variable
- routing policy: permission does not break the engine in the circuit breaker; re-dispatch the same brief once with --write when repository changes are acceptable, otherwise rewrite the brief to avoid shell commands

## 0.0.5

- the routing policy is rewritten around an operating model: the orchestrator runs the session like a founder with a paid bench, biases to fan out (five or more concurrent delegations is a normal state, under dispatching is the failure mode), keeps every engine drawing, and trusts then verifies instead of hovering; grok is named the fast lane and codex the deep lane
- peer engagement policy: peers are executors, not just reviewers. plans with three or more independent packages route at least one to a peer, multi source research fans one track to a peer by default, and a per session balance check shifts the next eligible package to an idle peer (motivated by observing sessions where codex and grok barely participated while claude workers took everything)
- auto invocation: the panel and the new fleet fire from plain language, not only a typed slash command. /fusion:panel's description is trigger shaped, and a new rules section maps natural language moments to both without the user typing a slash
- /fusion:ultra: the peer engine answer to ultracode. asking to go deep, thorough, or exhaustive fans the work out as a fleet of parallel Grok and Codex agents (6 to 8 facets by default, up to about 12), billed to their own subscriptions, then synthesized into one deliverable; a size gate skips the fleet for small tasks
- /grok:task gains --web, which re-enables grok's web tools for research briefs (they stay disabled for code work); grok-rescue's description now claims implementation packages and research digests first instead of introducing itself as a second opinion

## 0.0.4

- /fusion:stats aggregates delegation counts across both the grok and codex peers (grok via its stats subcommand, codex read best effort from its plugin job state), degrading each engine section independently when a peer is absent; token usage still lives with each vendor
- routing: implementation from an approved plan goes to fusion:fast-worker as one brief per work package, split rather than bundled, never to the generic catch-all agent; ordinary design uses the built in Plan agent while high stakes designs go to the panel
- fusion:fast-worker claims approved plan implementation in its description so it competes for that moment instead of ceding it to the catch-all (observed live: bundled phases went to the catch-all on the orchestrator's model while a single scoped package correctly went to fast-worker)

## 0.0.3

- readme and docs aligned with what the code actually enforces: billing wording softened, the safety model split into runtime enforced vs prompt requested, provenance anchors added to third party claims (grok 0.2.16, Claude Code 2.1.x)
- every companion job outcome now carries machine parseable state and failure lines, including spawn failures and cancellations (session end cancellations get failureKind cancelled)
- best-of-n rejects values outside 2 to 10 instead of forwarding any positive integer
- untracked files documented as reaching reviews by name only; timeout ladder and environment overrides documented in docs/grok-contract.md
- namespaced agent references (fusion:deep-reasoner and peers) unified across docs, rules, and the compaction hook; marketplace and plugin metadata unified under one author and description set
- added .gitignore, CI workflow, SECURITY.md, and this changelog

## 0.0.2

- tier agents and routing rules ship inside the fusion plugin; /fusion:setup installs the rules and /fusion:doctor audits drift, replacing install.sh
- stop gate parses BLOCK verdicts behind preamble lines; consult mode denies write tools even against inherited permission allows
- uniform state footer line on job outcomes; MIT license; readme rewritten around quick start

## 0.0.1

- initial release: grok plugin (companion runtime, background jobs, review, best-of-n, stop gate) and fusion plugin (panel, doctor), with the plain config orchestration layer
